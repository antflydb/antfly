apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../../manifests

patches:
  - path: antflycluster-metadata-replicas-cel.json
    target:
      group: apiextensions.k8s.io
      version: v1
      kind: CustomResourceDefinition
      name: antflyclusters.antfly.io
