# Antfarm Dashboard Configuration

# Optional API override. By default the dashboard uses same-origin /db/v1 and
# Vite proxies that to http://localhost:8080 in development.
VITE_ANTFARM_API_URL=
VITE_ANTFARM_INFERENCE_API_URL=
