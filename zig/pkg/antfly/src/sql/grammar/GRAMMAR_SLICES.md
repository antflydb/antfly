# Antfly SQL Grammar Remaining Slices

This is the tracking document for the SQL grammar migration. It lists only
remaining migration work and should stay checklist-shaped. Keep design
rationale, architecture decisions, ownership boundaries, and implementation
invariants in `GRAMMAR.md`. Completed history that is deducible from code,
tests, fixtures, or git history should stay out of both docs.

Completed read-plan and DML cutover slices are intentionally omitted from the
active checklist.

Feature work is listed first, followed by parser cutover, diagnostics, and
evidence/performance hardening. Check a slice only when the implementation,
diagnostics, and verification evidence are all in place.

Prefer generated AST dispatch and typed plan persistence over expanding local
token-layout interpreters. Tokenized payload hardening is acceptable for
correctness bugs in still-live compatibility paths, but it does not by itself
complete a generated-owned slice. Do not keep broadening tokenized payload
support where the remaining work is to retain, validate, lower, or persist a
generated AST/typed-plan shape.

When a remaining slice admits a new grammar terminal or promotes generated
syntax that production tokenization cannot already represent, keep the
production lexer path in the same patch: `TokenKind`/`TokenKeyword` in
`token.zig`, lexer recognition in `lexer.zig`, generated-token bridge mapping,
and focused lexer/parser fixtures. Prefer contextual keyword bridge handling for
generated-only words that must still be valid identifiers outside the new
grammar context. Lowering-only changes for terminals that production
tokenization already recognizes do not need lexer churn.

- [ ] Production parser cutover cleanup
  - [ ] Replace remaining DDL subject/body payload-local token parsing for subject-bearing families that are not yet generated-owned with retained generated payload lowering or deterministic unsupported diagnostics, keeping public-boundary corruption/parity evidence in the same patch as each payload family becomes generated-owned. Do not reintroduce public `sql.mod` grammar-syntax aliases, DDL token-tail parser aliases, or routine body compatibility parsers; residual DDL token parsers must stay private compatibility helpers until their family is deleted or replaced by retained generated metadata. Generated routine create/drop already lowers from retained routine metadata and validates body kind/hook payloads. Generated SQL-expression routine bodies now lower directly from retained body expression AST metadata, rewrite `$n`/`argN`/named arguments after retained lowering, validate retained body-expression payloads at tokenized and DDL catalog boundaries, and no longer synthesize normalized tokens for routine-body expression reparsing. Generated unsupported trigger lowering now retains structured trigger name/table/function/event/argument/drop-option metadata, validates it at tokenized and DDL catalog boundaries, and uses it for runtime and trigger-only public lowering instead of subject-local trigger field parsing; unsupported trigger shapes without retained trigger metadata remain deterministic unsupported. Generated notification lowering now retains channel/payload/all metadata for `LISTEN`, `NOTIFY`, and `UNLISTEN`, validates it at tokenized and DDL catalog boundaries, and uses it for public catalog lowering instead of subject-local notification parsing. Generated empty-argument `CALL` lowering now retains routine-name metadata, validates it at tokenized and DDL catalog boundaries, and uses it for runtime/logical procedure-call lowering instead of subject-local procedure parsing; non-empty calls remain deterministic unsupported. Generated table-lock lowering now retains table-name list and lock-mode metadata, validates it at tokenized and DDL catalog boundaries, and uses it for public transaction-control lowering instead of subject-local table-lock parsing. Generated maintenance lowering now retains `VACUUM` table/options, `ANALYZE` table/verbose/column-list, `REINDEX` target/name/concurrently, and `CLUSTER` table/index/verbose metadata, validates them at tokenized and DDL catalog boundaries, and uses them for public maintenance lowering instead of subject-local maintenance parsing. Generated authorization and unsupported-comment catalog fallbacks now rely on the shared catalog-boundary retained-AST validation and return deterministic unsupported without redundant subject-token fallback checks. Generated `COPY`/bulk IO now retains table, column-list, direction, endpoint, option-list/semantic option, and `WHERE` ranges, validates them at tokenized and DDL catalog boundaries, lowers public bulk IO plans from retained metadata, and deletes the final DDL `unsupported` subject-token parser family. Generated `BACKUP DATABASE`/`RESTORE DATABASE` admission now relies solely on generated unsupported AST classification; the tokenized generated-unavailable raw recovery path for those lifecycle diagnostics is deleted, with retained-AST corruption coverage for both shapes.
  - [x] Retire `CREATE INDEX` compatibility planning fallback. Ordinary non-derived `btree`/`gin` indexes now retain generated index element/include/WHERE expression metadata and lower directly for identifier keys, simple `lower(field)`/`upper(field)`/`md5(field)` unique/generated-expression keys, structured `concat`/`concat_ws` generated-expression keys, generated row-expression keys for retained AST arithmetic/string-concat/function/cast/literal/field payloads such as `replace(status, 'old', 'new')`, and unique temporal `WITHOUT OVERLAPS` period keys; derived `antfly_full_text`/`hnsw`/`antfly_aknn`/`antfly_graph`/`antfly_graph_metric`/`antfly_hybrid`/`antfly_algebraic` indexes lower from retained generated element and option ranges into config JSON. Current generated-owned coverage includes element order/nulls metadata, `gin`/HNSW opclass, `INCLUDE` columns, unique null policy, simple partial-index `UniquePredicate` predicates (`field = literal`, `field != literal`, `field IS NULL`, `field IS NOT NULL`, grouped `AND`), and simple generated expression partial predicates such as `lower(status) = 'active'`, with retained-list/expression/options corruption coverage and focused DDL/runtime coverage. The legacy whole-statement create-index parser and its grammar header/suffix helpers are deleted; unsupported generated CREATE INDEX constructs now fail closed instead of reparsing.
  - [ ] Future `CREATE INDEX` grammar expansion should add retained metadata/lowering or typed unsupported fixtures for unsupported expression constructs (for example JSON path/extract, arrays, case, temporal/current-value expressions) and broader complex partial-index predicates without reintroducing a whole-statement compatibility parser.
  - [ ] Replace remaining table-definition payload compatibility helpers with retained generated operation payloads for `ALTER TABLE ADD COLUMN` definitions, `CREATE TABLE` column definitions, generated/default expressions, table/column constraints, partition/clone/domain-related tails, and currently unsupported operation families. The whole-statement `parseAlterTablePlanAlloc` admission path is deleted: generated `ALTER TABLE` planning now owns table binding and the operation list from retained generated ranges, then exact-consumes each retained operation span. Simple generated-owned operation payloads (`VALIDATE CONSTRAINT`, `RENAME COLUMN/CONSTRAINT`, `DROP COLUMN/CONSTRAINT`, `ALTER COLUMN SET/DROP DEFAULT`, `ALTER COLUMN SET/DROP NOT NULL`, and `ALTER COLUMN TYPE/SET DATA TYPE`) now lower directly from retained generated operation spans; their grammar syntax structs and payload parsers are deleted. Generated `ALTER COLUMN ... TYPE/SET DATA TYPE ... USING ...` rewrite expressions now retain per-operation expression ranges and generated expression ASTs, validate that metadata at tokenized public, runtime DDL, and logical DDL boundaries, and lower from retained generated expression trees instead of falling back to DDL generated-expression token reparsing. ADD operation dispatch/prefix parsing is now local to generated ALTER TABLE planning, create-table/inline-constraint prefix parsing is local to DDL planning, and the exported grammar ADD/create-table prefix helpers are deleted. The dead `parseCreateTablePlanAlloc` wrapper, relation-lifetime whole-statement wrapper, generic `parseCreateTableDefinitionAlloc` parenthesized-body compatibility helper, and identity-allocator whole-body parser are deleted; generated `CREATE TEMP/TEMPORARY/UNLOGGED TABLE`, generated partitioned `CREATE TABLE ... PARTITION BY ...`, and generated identity-allocator `CREATE TABLE` now parse their table definitions through the retained generated definition item list instead of a whole-definition token parser. Generated simple relational `CREATE TABLE` now drives top-level definition item parsing from retained generated item ranges and validates retained definition item metadata before returning, so missing, stale, or corrupt generated item ranges fail closed at runtime and logical public boundaries instead of being masked by parenthesized-body token parsing. Stored generated column expressions now retain per-definition-item expression ranges and generated expression ASTs, validate that metadata at tokenized public, runtime DDL, and logical DDL boundaries, and lower from retained generated expression trees for simple `lower`/`upper`/`md5`, structured `concat`/`concat_ws`, supported row expressions, and typed routine-expression bindings; missing/stale retained generated-expression metadata and unbound/generated-unsupported function payloads fail closed, and the exported `grammar.parseDdlStoredGeneratedValueAlloc` compatibility wrapper plus its `sql.mod` aliases are deleted. Supported literal, known-function, and scalar-subquery defaults in generated `CREATE TABLE`, `ALTER TABLE ADD COLUMN`, and `ALTER COLUMN SET DEFAULT` now retain per-item default-value ranges, validate that metadata at tokenized public, runtime DDL, and logical DDL boundaries, and bound default parsing to the retained range so missing/stale retained default metadata fails closed instead of being masked by unbounded column/default token parsing. Generated CREATE TABLE header, identity-allocator table/spec, CREATE DOMAIN header, DDL type/default parsing, PERIOD constraint payload parsing, primary/unique constraint column-list/`INCLUDE`/`WITHOUT OVERLAPS` payload parsing, constraint timing parsing, and foreign-key column-list/options payload parsing are also local to DDL planning instead of exported grammar parsers; dead exported ALTER TABLE header/column-`USING`, DDL index wrapper, miscellaneous parser helper, routine-kind syntax shims, and public `sql.mod` grammar-helper aliases are deleted. The only remaining `sql.mod` grammar re-export is the HTTP/API-facing object identifier normalizer. Finish the slice by replacing ADD-column/CREATE TABLE column structure, broader default-expression semantics, and constraint payload-local parsers inside each retained item, plus typed unsupported tails, once SQL/API parity rows, runtime catalog coverage, and typed unsupported diagnostics cover the generated-owned surface.
    Generated `CREATE TABLE` and `ALTER TABLE ADD COLUMN` column definitions now retain per-item column-name, column-type, `NULL`/`NOT NULL`, and `COLLATE` ranges, validate that metadata at tokenized public, runtime DDL, and logical DDL boundaries, and bound column header/nullability/collation parsing to those retained ranges so missing/stale name/type/nullability/collation metadata fails closed before the column parser can rediscover those options from tokens. Generated column `DEFAULT` clauses now retain both the clause header/value boundary and the supported default-value payload, and stored generated column clauses now retain the full `GENERATED ALWAYS AS (...) STORED` clause plus the generated expression AST; both validate at tokenized public, runtime DDL, and logical DDL boundaries and lower from retained clause ranges before any column-option rediscovery can run. Generated `CREATE DOMAIN` options now retain per-option kind ranges, optional named-constraint identifiers, default-value payloads, and parenthesized CHECK payloads, validate those payloads at tokenized public, runtime DDL, and logical DDL boundaries, and lower domain defaults/nullability/checks from retained option metadata instead of rediscovering option kind/payload from the item start token. Generated `CREATE TABLE ... (LIKE source INCLUDING/EXCLUDING ...)` table-clone options now retain per-option action and option-name ranges, validate them at tokenized public, runtime DDL, and logical DDL boundaries, and lower clone options from retained ranges instead of rediscovering the action/name layout from each option item. Generated `CREATE TABLE ... WITH (...)` storage parameters now retain per-parameter key/value ranges, validate them at tokenized public, runtime DDL, and logical DDL boundaries, and lower document/relational storage-mode/default-type tails from retained metadata instead of rediscovering the `key = value` layout from each item. Generated `CREATE TABLE ... PARTITION BY RANGE (...)` keys now retain per-key identifier ranges, validate them at tokenized public, runtime DDL, and logical DDL boundaries, and lower partition keys from retained key metadata instead of generic list-item parsing. Generated unsupported `ALTER TABLE` table-option diagnostics now retain the operation-family kind alongside the table and operation ranges, plus the identifier operand for simple owner/tablespace/access-method/schema/inheritance/cluster-on forms; tokenized public and runtime/logical DDL validation reject stale or mismatched retained metadata before operation-shape validation can rediscover the family or operand from raw tokens. Generated `CREATE TABLE` table-level constraints and `ALTER TABLE ADD` constraints now retain per-item constraint-name and constraint-kind ranges, validate them at tokenized public, runtime DDL, and logical DDL boundaries, and dispatch generated prefix parsing from retained metadata so missing/stale table/ADD constraint prefix metadata fails closed before the DDL parser can rediscover the prefix from tokens. Generated `CREATE TABLE` and `ALTER TABLE ADD COLUMN` inline column constraints now retain flattened per-prefix item/name/kind metadata, validate it at tokenized public, runtime DDL, and logical DDL boundaries, and dispatch inline prefix parsing from retained metadata so missing/stale inline constraint prefix metadata fails closed before the column parser can rediscover `CONSTRAINT`, `PRIMARY KEY`, `UNIQUE`, `CHECK`, or `REFERENCES` prefixes from tokens. Generated table-level `CREATE TABLE`/`ALTER TABLE ADD` primary/unique constraints and inline `CREATE TABLE`/`ALTER TABLE ADD COLUMN` primary/unique constraints now retain their `NULLS DISTINCT`, key column-list where applicable, `INCLUDE`, timing, and `NOT VALID` payload ranges, validate them at tokenized public, runtime DDL, and logical DDL boundaries, and bound primary/unique payload parsing to those retained ranges so missing/stale column-list/include/timing/validation metadata fails closed before the DDL parser can rediscover those payloads from tokens. Generated table-level `CREATE TABLE`/`ALTER TABLE ADD` foreign-key constraints now retain their bounded `KEY ... REFERENCES ...` payload and trailing `NOT VALID` marker, and generated inline `CREATE TABLE`/`ALTER TABLE ADD COLUMN` `REFERENCES` constraints now retain their bounded reference payload and trailing `NOT VALID` marker; both validate that metadata at tokenized public, runtime DDL, and logical DDL boundaries, and bound FK payload parsing to the retained ranges so missing/stale FK metadata fails closed before the DDL parser can rediscover the payload from tokens. Generated table-level `CREATE TABLE`/`ALTER TABLE ADD` CHECK constraints and inline `CREATE TABLE`/`ALTER TABLE ADD COLUMN` CHECK constraints now retain their exact parenthesized CHECK payload and trailing `NOT VALID` marker, validate them at tokenized public, runtime DDL, and logical DDL boundaries, and bound CHECK parsing to those retained ranges so missing/stale CHECK metadata fails closed before the DDL parser can rediscover the payload from tokens. Generated table-level `CREATE TABLE`/`ALTER TABLE ADD PERIOD` constraints now retain their exact `FOR name (start, end)` payload, validate it at tokenized public, runtime DDL, and logical DDL boundaries, and bound PERIOD parsing to that retained range so missing/stale PERIOD metadata fails closed before identifier parsing can rediscover the payload. Remaining table-definition work is broader default-expression semantics, column-option typed unsupported tails for unsupported generated syntax, and residual unsupported partition/domain-related tails.
    Generated column-definition wrapper helpers for retained column name/type/nullability/collation/default/generated-clause metadata now fail closed when retained hooks are absent instead of falling back to unbounded column-definition token parsing. Generated column defaults and `ALTER COLUMN SET DEFAULT` now require retained default-value ranges before parsing bounded literal/known-function/scalar-subquery payloads, so missing retained default hooks no longer fall back to parsing the remaining local token stream. Generated `ALTER COLUMN ... USING ...` rewrite expressions and stored generated-column expression payloads now require retained generated expression AST metadata and no longer rebuild/reparse those expression payloads from local token slices when retained metadata is absent. Generated table/ADD and inline constraint wrapper helpers for retained `NULLS DISTINCT`, column-list, `INCLUDE`, timing, `NOT VALID`, FK payload, and CHECK payload metadata now fail closed when the retained hook is absent instead of falling back to unbounded constraint payload token parsing.
  - [ ] Replace remaining DML raw-token probes/reparse helpers for generated-owned assignments, sources, targets, conflict arms, returning projections, and document-write classifier paths with retained generated AST dispatch or deterministic unsupported diagnostics. Generated `TRUNCATE` lowering already uses retained generated DML metadata for target binding, additional target validation, identity/cascade flags, corruption coverage, and runtime/source tests; its internal lowerer DTO is no longer a public grammar syntax type. Generated `INSERT ... SELECT` source slicing now uses retained `source_tokens` when a generated DML AST owns the statement, so stale source-boundary metadata fails closed and the old raw top-level `RETURNING`/`ON CONFLICT` SELECT-boundary scan is deleted; the generated insert-source entrypoint also requires retained `insert_column_items`, so the raw insert-column identifier-list parser branch is deleted, and CTE source-schema resolution reads retained `source_read.source_table_tokens` instead of scanning the SELECT body for top-level `FROM`. Generated insert-source conflict/returning tail parsing now dispatches from retained `conflict_tokens`/`returning_tokens` at the entrypoint instead of token-matching `ON`/`RETURNING`, with missing/stale conflict and returning metadata coverage. Generated insert-values/source and shared DML tail validators now rely on exact retained `conflict_tokens`/`returning_tokens`/range boundaries instead of duplicate top-level `ON CONFLICT`/`RETURNING` scans, with missing-conflict and swallowed-returning corruption coverage. Generated returning projection parsing now consumes simple-field aliases from retained returning-list alias ranges instead of reparsing optional `AS` aliases from tokens, with simple-field and expression stale-alias corruption coverage. Document-write returning projection aliases now also consume retained returning-list alias ranges for insert/update/delete/merge document paths, with stale document returning-alias corruption coverage. Generated shared mutation-source query tails and joined mutation tails now dispatch `WHERE`, `ORDER BY`, `LIMIT`, `OFFSET`, `FETCH`, row locks, and `RETURNING` from retained mutation tail ranges instead of raw clause keyword matches; shared mutation tail validation now derives the `WHERE` predicate boundary from retained `where_expression.tokens`, walks the retained clause sequence contiguously, and rejects missing/stale retained predicate or pagination/row-lock metadata before any tail separator rediscovery can occur. Generated semijoin/existence source selection consumes `WHERE` from retained `where_tokens`, dispatches by retained expression kind, consumes subquery-local filters from retained `subquery_where_tokens`, rejects generated-owned subquery-local `WHERE` tokens when retained predicate metadata is absent, and enters row-lock/returning parsing only from retained `mutation_row_lock_tokens`/`returning_tokens`; focused retained-hook and broad generated-DML coverage are green. Generated `MERGE` clause parsing and range validation now derive `RETURNING`, first-arm placement, per-arm `THEN` boundaries, action spans, and not-matched insert column/value lists from retained DML/merge-arm metadata instead of token-matching/scanning in the clause parser or reparsing insert columns through the grammar identifier-list helper, with focused merge hook and stale insert-column/value metadata coverage. Generated named conflict targets (`ON CONSTRAINT name`) now extract the constraint identifier from the retained `conflict_target_tokens` layout instead of reparsing it through the grammar identifier helper, with retained-range corruption coverage. Generated conflict-target `WHERE` predicates now require retained token range and retained expression metadata to be present together before any token predicate parsing runs, and generated conflict-action `WHERE` predicates now consume only from retained `conflict_action_where_tokens` before invoking predicate parsing; generated conflict validation now also derives target/action `WHERE` and action boundaries from retained ranges instead of scanning for top-level `DO`/`WHERE`, so missing or stale retained conflict `WHERE` metadata fails closed before separator rediscovery. Generated boolean assignment/bare-conflict expression dispatch now requires retained generated boolean expression metadata at the current token and no longer scans assignment/WHERE tails for boolean operators; the related `sql.mod` can-parse aliases are deleted. Generated point update/delete selector predicates now require each parsed atom to start from retained generated predicate metadata before token selector parsing can admit it. Document-write operation classification now validates retained generated DML target/source/value/assignment/where/merge payload presence before choosing an operation, with stale target, assignment span, delete predicate span, and joined-source metadata corruption coverage. Document insert lowering now extracts column names from retained `insert_column_items`, exact-consumes retained `insert_value_rows`, requires retained `source_tokens` for `INSERT ... SELECT`, derives source-projection fields from retained `source_read.projection_items` instead of retokenizing projection strings, derives values-path and source-projection conflict/returning tail boundaries from retained `conflict_tokens`/`returning_tokens`, derives source-projection `ORDER BY` rejection and supported `LIMIT` from retained `source_read.order_tokens`/`limit_tokens` plus retained `WHERE` expression boundaries instead of raw source-tail scans, consumes document conflict target/action/assignment payloads from retained `conflict_target_tokens`/kind/items, `conflict_action_tokens`/kind, `conflict_assignments_tokens`/items, and validated conflict assignment expression metadata, derives conflict source assignments through the generated-backed assignment-expression path instead of the deleted document-specific `excluded.field` shortcut, and threads retained `conflict_action_where_tokens` into document conflict predicate parsing before tail parsers run for both values and source-projection inserts, with stale values-range, row-item, source-range, source-projection, source-limit, conflict-tail, conflict-target, conflict-action, conflict-assignment, conflict-assignment-expression, and returning-tail corruption coverage. Exact document delete lowering now requires retained target and `WHERE` expression metadata before selector parsing, with stale target and missing predicate corruption coverage. Non-identity document producer delete lowering now validates retained delete source ranges, target binding, `WHERE` expression metadata, and `RETURNING` metadata before producer/returning parsing, with missing predicate, stale predicate-expression, and missing returning corruption coverage. Document patch update lowering now bounds `_doc = antfly.json_patch(...)` parsing to the retained assignment item, validates retained assignment expression metadata, requires retained `WHERE` expression metadata before identity parsing, and rejects retained `RETURNING` metadata deterministically, with stale assignment-range, assignment-item, predicate, and returning corruption coverage. Document projection update lowering now bounds assignment parsing to retained `assignment_items`, requires retained `WHERE`/expression metadata before identity/producer parsing, and derives returning boundaries from retained `returning_tokens` before returning projection parsing instead of scanning the token stream, with stale assignment-range, assignment-item, predicate, and returning corruption coverage. Joined document projection update/delete lowering now validates retained joined source ranges, assignment/source/target binding, retained `WHERE` expression metadata, and retained `RETURNING` metadata before joined predicate/returning parsing, with stale source, assignment, predicate-expression, and returning corruption coverage. Document `MERGE` lowering now requires retained generated merge ranges, source metadata, merge-arm metadata, and retained `RETURNING` metadata before shared merge/document returning parsing, and document merge expression-schema dynamic fields are derived only from retained generated `ON`, arm predicate/action, and returning ranges instead of a statement-wide identifier scan, with stale source, missing arms, stale arm action, and missing returning corruption coverage. Public parsed-SQL DML ingress is covered against direct retained generated-DML lowering for representative generated write families. Joined/semi-join mutation tail parser options and parsed-tail DTOs are now private to DML lowering; conflict expression start probes and intermediate merge/semi-join parsed DTOs are private to DML lowering; all `lower_dml` parser option/hook/context aliases, generated mutation source parser aliases, conflict parser aliases, and intermediate parsed-source DTO aliases are deleted from `sql.mod`. Plan-level tail hooks remain internal SQL package helpers only. Finish the slice by deleting or privatizing the remaining DML token probes and expression-parser DTOs once each family has public-boundary corruption coverage, SQL/API parity or typed unsupported rows, and focused runtime coverage.
    Joined document projection update/delete source aliases now bind from retained `source_read` metadata instead of reparsing source table aliases from token ranges, with existing stale source-alias corruption coverage.
    Document projection update assignment values now validate retained assignment RHS expression metadata before document value parsing can consume the token payload, with stale assignment-expression corruption coverage.
    Document identity filters for exact delete, patch update, and projection update now require retained generated predicate atom ranges for `_id`/`_version` equality and `_id IN (...)` before token-level field/value parsing, with stale multi-atom identity-filter corruption coverage.
    Joined document projection update/delete predicate sets now require retained generated predicate atom and child-boundary ranges before dispatching join-key, version, exact target-producer, or bounded target-producer token parsers, with stale multi-atom and stale child-range joined-predicate corruption coverage.
    Document insert-source, projection update, and producer delete bounded producer filters now reject `_doc`/`_version` by inspecting retained generated `WHERE` expression metadata instead of scanning the raw producer token slice.
    Document insert/update/delete target aliases now bind from retained generated target metadata instead of reparsing DML target aliases in document insert, exact delete, producer delete, patch update, projection update, and joined projection update/delete lowering paths, with stale target-alias corruption coverage for generated insert-source, update, delete, and joined write boundaries.
    Document insert-source source aliases now bind from retained `source_read` metadata instead of reparsing the `FROM` table alias, with stale source-alias lowering coverage; the old DML target-alias parser is no longer re-exported through `sql.mod`.
    Conflict `excluded` field parsing now admits only the generated qualified-identifier form (`excluded.field`) and no longer accepts the older whitespace-separated `excluded field` recovery path.
    Generated `IN (SELECT ...)` and `EXISTS (SELECT ...)` semijoin source headers now validate retained subquery projection/source metadata and parse source fields only within retained projection item ranges instead of scanning the subquery projection until `FROM`, with stale projection-list corruption coverage.
    Low-level parser helper aliases (`parseTableAliasAlloc`, `parseQualifiedFieldAlloc`, `parseJoinProjectionListAlloc`, top-level keyword/tail scanners, token/keyword matchers, CTE shape token probes, unique-predicate condition parser aliases/options, expression WHERE/order/returning/group parser aliases, aggregate delimiter/having parser aliases, and range/array predicate parser aliases), read-plan parser entrypoint/hook/context aliases, and expression/read/DML parser option/function aliases are no longer re-exported through `sql.mod`; current generated-owned lowerers use retained metadata or private package helpers directly.
    Conflict/merge parser probes and insert-source conflict clause conversion helpers are now private to DML lowering and no longer re-exported through `sql.mod`.
    Document SQL native `antfly.*` search predicates now classify and lower from retained generated function-call expression metadata and generated argument-item ranges instead of scanning the raw `WHERE` clause for qualified function names and reparsing the raw function body; stale function kind or argument-range metadata fails closed with focused retained-AST coverage. Generated-read and aggregate bare `full_text_search(...)` producers now lower the query body from retained generated function-call argument metadata instead of reparsing the raw clause body, with stale argument range/kind metadata failing closed. Document-write producer lowering now threads retained generated `WHERE` expression metadata into the shared document producer helper instead of calling a token-only producer parser. Generated read/aggregate/write producer lowering now requires retained `WHERE` expression metadata before full-text/native producer planning. Generated-read and aggregate exact-producer error classification for bare `full_text_search(...)` and native `antfly.*` predicates now uses retained generated expression metadata before translating index-unavailable into `DocumentSqlBoundedScanMissingExactProducer`, with stale expression metadata and missing retained `WHERE` hooks failing closed instead of being recovered by a raw WHERE-token scan; the old exact-producer raw WHERE scanner, qualified-name probe, dead raw full-text clause parser, and dead whole-clause parser are deleted. Raw Antfly query-function parser helpers are private to `query_function.zig` and are no longer re-exported through `sql.mod`; public entrypoints stay on SQL/ParsedSql lowering or retained generated AST lowering.
  - [ ] Do not reintroduce tail parsers for already generated-owned catalog families. Role statement admission must not reintroduce create/alter/drop role tail parsers; authorization statement admission must not reintroduce privilege, role-privilege, or default-privilege tail parsers; type-system statement admission must not reintroduce collation/operator/aggregate/cast tail parsers; namespace/database/extension/tablespace, sequence/enum-type, view/materialized-view, logical-replication, row-security, comment/security-label, bulk IO, table clone/partition, relational create-table tails, relation-population (`CREATE TABLE AS`/`SELECT INTO`) lowering, alter/drop domain, drop table/index, session setting/search-path/discard, transaction mode/constraint-mode, advisory-lock, protocol/cursor/savepoint, notification/maintenance/procedure-call/table-lock, routine, and trigger statement admission must stay on retained generated metadata or deterministic generated unsupported diagnostics. Relation-population lowering now keeps its generated-source metadata DTO private to read planning, and relation lifetime is a DDL-plan type rather than public grammar syntax.
  - [ ] Complete source-less native graph row/path execution for `MATCH ... RETURN alias/path` rows. Source-bound `MATCH ... WITH GRAPH ... ON ... START ... RETURN ...` is generated-owned at public SQL/API and prepared-statement ingress, lowers through retained native graph metadata to `antfly.graph_match`, and has SQL/API parity plus retained-AST corruption coverage. Source-less path-return, filtered-return, incoming, multi-return, and ordered/limited graph rows intentionally remain deterministic typed `graph_query_plan` unsupported diagnostics until they have executable graph-row/path row shape and SQL/API parity.
  - [ ] For each remaining cutover deletion, add tokenized public-boundary retained-AST corruption tests, SQL/API parity or typed unsupported rows, required native/unsupported manifest updates, and focused runtime tests in the same patch.
  - [ ] Run fixture promote/check, relevant unsupported-reason checks, focused retained-AST corruption tests, and full SQL/API parity before marking a fallback family removed.

- [ ] Native subquery semantics
  - [ ] Finish default-expression scalar-subquery coverage beyond the current retained child-read subset. Current native evidence covers persisted structured `scalar_subquery` defaults, direct retained generated child-read lowering to structured DDL query payloads for both `CREATE TABLE` and `ALTER TABLE ... SET DEFAULT`, DDL planning that rejects scalar-subquery defaults without matching retained generated child-read metadata instead of reparsing through the grammar default normalizer, row-write resolvers without per-row plan normalization for structured defaults, single-field projections, simple expression projections (`lower`/`upper`/`length`/`md5`/`reverse(field) [AS alias]`, `concat(field, literal) AS alias`), retained generated arithmetic projection expression trees for scalar defaults (`field + literal` and nested arithmetic payload normalization), retained generated `CAST(field AS type)` projection expression trees, supported filters/order/pagination metadata, retained child-read corruption coverage at the generated lowerer and tokenized public boundary for admitted projection/source/filter/order/pagination/read-kind metadata, schema serialization, schema derivation and schema JSON emission normalization for persisted structured payloads, Lite/public resolver execution, and SQL/API parity rows for create/alter scalar-subquery defaults. The remaining schema JSON normalizer is structured-payload validation/canonicalization, not a legacy SQL-text parser fallback.
  - [ ] Broaden scalar-subquery default child-read binding to generated projection expression trees, source/table shapes, CTEs, set operations, aggregates/windows, and multi-output rejection/cardinality diagnostics without raw SQL reparsing.
  - [ ] Extend retained-AST corruption coverage for scalar-subquery default child-read payloads as broader generated-owned default expressions are admitted, including future CTE, set-operation, aggregate/window, multi-output, and richer generated expression-tree payloads.
  - [ ] Complete generated subquery expression payload lowering outside the now-covered read projection/predicate/order paths: assignments, defaults, generated expressions, aggregate/window payloads, and document SQL expression entrypoints must lower directly from retained child read ASTs or fail closed before legacy expression parsing.

- [ ] Function and expression semantic completion
  - [ ] Promote cast and type-coercion lowering from retained generated expression payloads across projections, predicates, assignments, defaults, generated expressions, aggregate/window payloads, and document SQL expression entrypoints. Document SQL direct-table and simple mapped-view scalar `CAST(field AS text)` projections now lower to typed projection/runtime casting for numeric, boolean, datetime, and text-like scalar fields, scalar `CAST(field AS numeric)` projections now lower to typed projection/runtime casting for numeric and text-like scalar fields, scalar `CAST(field AS boolean)` projections now lower to typed projection/runtime casting for boolean and text-like scalar fields, and scalar `CAST(field AS timestamp)`/`datetime`/`date` projections now lower to typed projection/runtime casting for integer-nanosecond scalar fields; residual/bounded-scan direct-table and simple mapped-view scalar `CAST(field AS text)` equality predicates, scalar `CAST(field AS numeric)` comparison predicates, scalar `CAST(field AS boolean)` equality predicates, and scalar `CAST(field AS timestamp)`/`datetime`/`date` comparison predicates now lower to typed planning/runtime casting for supported scalar fields; broader cast/type-coercion entrypoints still fail closed until source projection binding and generated expression lowering are complete.
  - [ ] Promote array expression lowering from retained generated expression/list payloads across projections, predicates, assignments, defaults, generated expressions, aggregate/window payloads, and document SQL expression entrypoints. Document SQL direct-table and simple mapped-view projections now have typed planning/runtime coverage for `cardinality`, `array_length(array_field, 1)`, `array_append`, `array_prepend`, `array_cat`, `array_remove`, `array_replace`, `array_position`, `array_positions`, `array_to_string`, and `string_to_array`; direct-table and simple mapped-view `array_position(array_field, literal) > 0`/`>= 1`/`!= 0`/`IS NOT NULL` membership predicates lower to native `array_any`, `IS NULL` lowers to negated native `array_any`, `array_field && ARRAY[...]` overlap predicates lower to native `array_any`/disjunction filters, `array_field @> ARRAY[...]` contains predicates lower to native `array_any`/conjunction filters, and residual/bounded-scan `cardinality(array_field)`/`array_length(array_field, 1)` comparison predicates lower to typed `array_length_range`; broader generated array expression entrypoints still fail closed until generated expression lowering is complete.
  - [ ] Promote temporal literal lowering from retained generated expression payloads across projections, predicates, assignments, defaults, generated expressions, aggregate/window payloads, and document SQL expression entrypoints. Document SQL direct-table and simple mapped-view `date_utc(datetime_field)` projections now lower to typed projection/runtime UTC date materialization, and direct-table/simple mapped-view `date_utc(datetime_field)` predicates lower to typed UTC date-range filters; broader temporal expression entrypoints still fail closed until generated expression lowering is complete.
  - [ ] Promote generic scalar function-call lowering from retained generated argument-list payloads across projections, predicates, assignments, defaults, generated expressions, aggregate/window payloads, and document SQL expression entrypoints. Document SQL direct-table and simple mapped-view projections now have typed planning/runtime coverage for text length, ASCII case folding, `substring`/`substr`, `overlay`, `left`, `right`, `repeat`, `reverse`, default `btrim`/`ltrim`/`rtrim`/`trim`, `lpad`, `rpad`, `split_part`, `starts_with`, `ends_with`, `strpos`, `position`, `octet_length`, `bit_length`, `ascii`, `chr`, `initcap`, `md5`, `soundex`, `replace`, `nullif`, `concat_ws`, regexp count/instr/substr, `translate`, numeric `abs`, numeric literal arithmetic including `%` modulo, numeric `MOD(field, literal)`, numeric `round`/`trunc`/`floor`/`ceil`/`sqrt`/`sign`, numeric `power`, and `json_typeof`/`jsonb_typeof` plus `json_array_length`/`jsonb_array_length` over JSON-valued fields and paths. Residual/bounded-scan direct-table and simple mapped-view predicates now have typed planning/runtime coverage for text length, `octet_length`, `bit_length`, `nullif(text_field, literal)` equality, `replace(text_field, literal, literal)` equality, `translate(text_field, literal, literal)` equality, `concat_ws(literal, text_field, text_field)` equality, `regexp_count(text_field, literal)`/`regexp_instr(text_field, literal)` comparisons, `regexp_substr(text_field, literal)` equality, `chr(numeric_field_or_integer_literal)` equality, `lpad(text_field, width[, fill])`/`rpad(text_field, width[, fill])` equality, `split_part(text_field, literal, index)` equality, `starts_with(text_field, literal)`/`ends_with(text_field, literal)` boolean predicates, `strpos(text_field, literal)`/`position(literal in text_field)` comparisons, `ascii(text_field)` comparisons, `initcap(text_field)`/`md5(text_field)`/`soundex(text_field)` equality, `reverse(text_field)` equality, default `trim`/`btrim`/`ltrim`/`rtrim` equality, `repeat(text_field, count)` equality, `left(text_field, count)`/`right(text_field, count)` equality, `substring`/`substr` equality, `overlay(text_field placing literal from start [for length])` equality, numeric `round`/`trunc`/`floor`/`ceil`/`sqrt`/`sign`, numeric `power`, `%` modulo, `MOD(field, literal)`, `json_typeof`/`jsonb_typeof` equality, and `json_array_length`/`jsonb_array_length` comparisons. Broader generated scalar function entrypoints still fail closed until generated expression/source binding is complete.
  - [ ] Remove token-layout fallback where retained generated expression/list payloads are available, or fail closed with typed unsupported diagnostics.
  - [ ] Lower arithmetic, comparison, boolean, and null-safe operators from generated operator metadata. Document SQL direct-table and simple mapped-view scalar filters now lower boolean composition for grouped `OR`, grouped `NOT (...)`, and `NOT IN` literal lists through typed filter JSON, with focused fail-closed planning coverage for malformed grouped `OR`, ungrouped `NOT`, malformed `NOT IN` literal lists, and retained generated `WHERE` operator-token, child-range, and child-kind corruption; document SQL read-plan corpus coverage for indexed mapped-view residual `NOT`, `NOT IN`, and grouped `OR`; and document SQL corpus runtime coverage for emitted `bool.must_not` `terms` residuals. Simple mapped-view public SQL/API parity now covers bounded residual `NOT`, `NOT IN`, and grouped `OR` rows. Broader generated operator entrypoints still fail closed until source projection binding and generated expression lowering are complete.
  - [ ] Lower pattern, regex, JSON/path, containment, overlap, and array operators from generated operator metadata. Document SQL direct-table and simple mapped-view pattern/regex predicates now lower `LIKE`/`ILIKE`, `~`/`~*`, and `!~`/`!~*` through typed residual filters behind bounded or exact producers, with document SQL read-plan corpus coverage for indexed mapped-view residual `ILIKE`, regex match, and negated case-insensitive regex match plus retained generated `WHERE` operator-token, child-range, and child-kind corruption coverage; simple mapped-view public SQL/API parity now covers bounded residual `ILIKE`, regex match, and regex negated case-insensitive match rows. Document SQL direct-table and simple mapped-view JSON key-exists predicates now lower `json_field ? 'key'` to native `exists`, `json_field ?| ARRAY[...]` to `exists` disjunctions, and `json_field ?& ARRAY[...]` to `exists` conjunctions, with document SQL read-plan corpus coverage, public SQL/API parity rows, required coverage buckets for indexed mapped-view residual key-exists/key-any/key-all rows, and the same retained generated `WHERE` corruption coverage. Document SQL direct-table and simple mapped-view array overlap/contains predicates now lower `array_field && ARRAY[...]` to typed disjunction residual filters and `array_field @> ARRAY[...]` to typed conjunction residual filters, with document SQL read-plan corpus coverage, public SQL/API parity rows, required coverage buckets for indexed mapped-view residual overlap/contains rows, and the same retained generated `WHERE` corruption coverage. Non-document expression entrypoints still fail closed until source projection binding and generated expression lowering are complete.
  - [ ] Add public SQL/API parity or typed unsupported rows for each generated-owned operator family.
  - [ ] Add retained-AST corruption coverage for stale operator kind, child range, and child-expression metadata for each generated-owned operator family.
  - [ ] Complete aggregate/function planning from generated argument lists, variadic arguments, casts, and type coercion.
  - [ ] Cover aggregate/function executable row shapes and deterministic unsupported diagnostics for admitted-but-unimplemented shapes.
  - [ ] Audit SELECT projections, WHERE/HAVING, GROUP BY, ORDER BY, and RETURNING to either lower directly from retained generated payloads or reject malformed/stale payloads before legacy expression parsing.
  - [ ] Audit document SQL filters/projections, aggregate payloads, and window payloads to either lower directly from retained generated payloads or reject malformed/stale payloads before legacy expression parsing.
  - [ ] For any newly admitted expression operator, keyword, or punctuation, update `TokenKind`/`TokenKeyword` in `token.zig`, lexer recognition in `lexer.zig`, generated-token mapping, and focused lexer/parser fixtures before relying on generated expression lowering.
  - [ ] Keep SQL/API parity rows, typed unsupported rows, required coverage or unsupported-reason manifests, and focused fail-closed corrupted child-expression/list payload fixtures in the same patch as each expression-family promotion.

- [ ] Evidence and performance hardening
  - [ ] Keep tracking bullets checkable at end-to-end behavior granularity. Split oversized bullets when a subset can be completed independently, and remove completed-history detail once code, fixtures, tests, or git history are authoritative.
  - [ ] For each generated-owned SQL shape admitted at the public SQL/API boundary, add or graduate rows in `fixtures/sql_api_parity_source_corpus.json`, promote `fixtures/sql_api_parity_corpus.json`, and update required coverage or unsupported-reason manifests in the same patch.
  - [ ] When a required coverage bucket relies on native-equivalence metadata derived during fixture promotion, either pin explicit native evidence in the source row or make the source-corpus observer assert the structural/public-plan contract without requiring explicit source evidence.
  - [ ] For Antfly-specific query functions, graph, Lite, table APIs, and routed execution paths, add SQL/API parity rows when the public adapter contract changes and focused Zig runtime tests when seeded execution behavior is the proof.
  - [ ] For document SQL-specific read/write behavior, use `fixtures/document_sql_corpus.json` as the canonical behavior fixture, update `fixtures/sql_document_dependency_guard.json`, keep the document plan/runtime corpus tests passing, and still include lexer/generated-token bridge fixtures in the same patch when the document SQL shape admits new production syntax.
  - [ ] Add SQL/API corpus rows for document SQL only when public adapter routing, coverage buckets, unsupported reasons, or plan summaries change.
  - [ ] For document SQL bounded-scan compatibility, update `fixtures/document_sql_bounded_scan_inventory.json` only when changing admitted scan contracts and keep matching SQL/API parity rows in the same patch.
  - [ ] For document SQL read expansion, update `fixtures/document_sql_read_expansion_gate.json` only when changing the `additional-array-unnest-patterns` gate and keep matching SQL/API parity rows in the same patch.
  - [ ] Add deterministic malformed SQL diagnostics for each touched family, with parser diagnostics and public SQL adapter fail-closed coverage in the same patch.
  - [ ] Keep short mutation/fuzz coverage in default tests; when parser fuzzing changes, update the `sql-parser-fuzz` seed/replay workflow in the same patch.
  - [ ] Before production cutover, record parser throughput, allocation count, parse-table size, compile-time impact, and binary size from parser benchmarks.
  - [ ] Before marking any migrated family complete, run the relevant SQL/API parity check, unsupported-reason fixture check, and fail-closed retained-AST corruption tests.
