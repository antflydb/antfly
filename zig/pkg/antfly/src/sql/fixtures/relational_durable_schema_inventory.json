{
  "inventory_format": 1,
  "entries": [
    {
      "id": "concurrent-writer-generation-isolation",
      "relational_slices_section": "Durable schema and migration execution",
      "surface": "concurrent_writer_generation_isolation",
      "status": "release_evidence",
      "current_evidence": "Metadata table updates with schema rewrite jobs apply atomically, schema progress waits for hosting nodes, schema rewrite workers invalidate stale range jobs before rewriting rows, and table-emptying barriers validate complete ready range jobs before data-generation or restart-identity promotion. The worker-level generation-isolation workflow now drives live row rewrite, CAS promotion, stale table-emptying invalidation, restart-identity table emptying, and reopen together: a v1 row is rewritten to v2, the DB reopens before promotion, table schema CAS removes the read schema, a stale v1 emptying job fails closed without deleting the rewritten row, a current v2 restart-identity emptying job deletes both rewritten and post-promotion rows, barrier promotion advances data generation and resets the identity allocator, and final reopen observes no mixed-generation rows.",
      "missing_evidence": "",
      "evidence_file": "zig/pkg/antfly/src/api/table_writes/schema_jobs.zig",
      "evidence_symbol": "schema rewrite and table emptying isolate generations across reopen",
      "release_gate": "relational-release-gate"
    },
    {
      "id": "create-or-replace-table-schema-job",
      "relational_slices_section": "Durable schema and migration execution",
      "surface": "create_or_replace_table",
      "status": "release_evidence",
      "current_evidence": "Applied SQL DDL supports CREATE OR REPLACE TABLE catalog application, marks rebuild/validation/rewrite work, deterministically schedules typed, row-plan, and full-row schema rewrite jobs from applied DDL work, persists table updates with rewrite jobs atomically across metadata reopen, and promotes completed rewrite work through table schema CAS. The raft apply-store workflow now ties those pieces together by applying a replacement table with read-schema fallback plus rewrite and validation jobs, reopening with pending work, invalidating and retrying row rewrite work, completing validation, promoting through table schema CAS, deleting completed jobs, reopening on the promoted schema, and replaying the promotion command idempotently.",
      "missing_evidence": "",
      "evidence_file": "zig/pkg/antfly/src/metadata/storage/raft_apply_store.zig",
      "evidence_symbol": "metadata raft apply store replays create-or-replace schema rewrite workflow idempotently",
      "release_gate": "relational-release-gate"
    },
    {
      "id": "drop-table-cascade-recoverable-job",
      "relational_slices_section": "Durable schema and migration execution",
      "surface": "drop_table_cascade",
      "status": "release_evidence",
      "current_evidence": "SQL DROP TABLE CASCADE updates child schemas and records catalog work items, while table-emptying barriers schedule cascade-aware multi-table jobs, repair jobs after range topology changes, wait for every affected table range, reset identity allocators, promote data generations, and remove completed durable jobs. The raft apply-store workflow now combines those pieces by emptying affected parent and child ranges under one cascade/restart-identity barrier, reopening after partial progress with promoted data generations and reset identity allocators, applying the catalog drop to update the child schema, remove the dropped table range, remove the dropped table sequence, preserve the child range and reset child sequence, persist child rewrite cleanup work, and reopen with no stale table-emptying jobs.",
      "missing_evidence": "",
      "evidence_file": "zig/pkg/antfly/src/metadata/storage/raft_apply_store.zig",
      "evidence_symbol": "metadata raft apply store recovers drop cascade through emptying and catalog cleanup",
      "release_gate": "relational-release-gate"
    },
    {
      "id": "schema-job-operator-controls",
      "relational_slices_section": "Durable schema and migration execution",
      "surface": "operator_controls",
      "status": "release_evidence",
      "current_evidence": "Schema rewrite and table-emptying jobs expose durable lifecycle operations, progress records, stale lease rejection/takeover through lease expiry, wake loops, busy-only worker passes, invalidation with last_error, HTTP worker routes, admin-snapshot job visibility, and typed metadata manager/service/catalog-source controls for pause, resume, retry/requeue, and cancel. Raft transition commands persist those operator controls across reopen for both job families, and internal metadata HTTP routes expose all eight controls with deterministic 404/405/409 response classes. Worker passes now share an explicit admission policy for lease TTL, per-pass work bounds, and stale-running-job admission; focused coverage proves both job families honor the bounds and can disable or allow stale lease takeover admission.",
      "missing_evidence": "",
      "evidence_file": "zig/pkg/antfly/src/api/table_writes/schema_jobs.zig",
      "evidence_symbol": "schema job worker admission policy bounds work and stale takeover; metadata raft apply store persists schema and table emptying operator controls across reopen",
      "release_gate": "relational-release-gate"
    }
  ]
}
