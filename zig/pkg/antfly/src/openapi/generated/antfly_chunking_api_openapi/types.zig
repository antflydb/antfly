// Code generated by openapi-zig. DO NOT EDIT.
// Package: antfly_chunking_api_openapi

const std = @import("std");

/// Options specific to audio chunking.
pub const AudioChunkOptions = struct {
    /// Window duration in milliseconds for fixed-window audio chunking (default: 30000).
    window_duration_ms: ?i64 = null,
    /// Overlap duration in milliseconds between audio chunks (default: 0).
    overlap_duration_ms: ?i64 = null,

    pub fn jsonParse(allocator: std.mem.Allocator, source: anytype, options: std.json.ParseOptions) !@This() {
        return try openApiParseObject(@This(), &.{
            .{ .json_name = "window_duration_ms", .zig_name = "window_duration_ms", .rejects_null = true },
            .{ .json_name = "overlap_duration_ms", .zig_name = "overlap_duration_ms", .rejects_null = true },
        }, allocator, source, options);
    }

    pub fn jsonParseFromValue(allocator: std.mem.Allocator, source: std.json.Value, options: std.json.ParseOptions) !@This() {
        return try openApiParseObjectFromValue(@This(), &.{
            .{ .json_name = "window_duration_ms", .zig_name = "window_duration_ms", .rejects_null = true },
            .{ .json_name = "overlap_duration_ms", .zig_name = "overlap_duration_ms", .rejects_null = true },
        }, allocator, source, options);
    }

    pub fn jsonStringify(self: @This(), jw: anytype) !void {
        try jw.beginObject();
        if (self.window_duration_ms) |value| {
            try jw.objectField("window_duration_ms");
            try jw.write(value);
        }
        if (self.overlap_duration_ms) |value| {
            try jw.objectField("overlap_duration_ms");
            try jw.write(value);
        }
        try jw.endObject();
    }
};

/// Binary media content with format-specific metadata.
pub const BinaryContent = struct {
    /// Base64-encoded binary data (valid WAV, PNG, etc.)
    data: ?[]const u8 = null,
    /// Audio: window start time in milliseconds
    start_time_ms: ?f32 = null,
    /// Audio: window end time in milliseconds
    end_time_ms: ?f32 = null,
    /// Animation: frame number
    frame_index: ?i64 = null,
    /// Animation: display delay in milliseconds
    frame_delay_ms: ?i64 = null,

    pub fn jsonParse(allocator: std.mem.Allocator, source: anytype, options: std.json.ParseOptions) !@This() {
        return try openApiParseObject(@This(), &.{
            .{ .json_name = "data", .zig_name = "data", .rejects_null = true },
            .{ .json_name = "start_time_ms", .zig_name = "start_time_ms", .rejects_null = true },
            .{ .json_name = "end_time_ms", .zig_name = "end_time_ms", .rejects_null = true },
            .{ .json_name = "frame_index", .zig_name = "frame_index", .rejects_null = true },
            .{ .json_name = "frame_delay_ms", .zig_name = "frame_delay_ms", .rejects_null = true },
        }, allocator, source, options);
    }

    pub fn jsonParseFromValue(allocator: std.mem.Allocator, source: std.json.Value, options: std.json.ParseOptions) !@This() {
        return try openApiParseObjectFromValue(@This(), &.{
            .{ .json_name = "data", .zig_name = "data", .rejects_null = true },
            .{ .json_name = "start_time_ms", .zig_name = "start_time_ms", .rejects_null = true },
            .{ .json_name = "end_time_ms", .zig_name = "end_time_ms", .rejects_null = true },
            .{ .json_name = "frame_index", .zig_name = "frame_index", .rejects_null = true },
            .{ .json_name = "frame_delay_ms", .zig_name = "frame_delay_ms", .rejects_null = true },
        }, allocator, source, options);
    }

    pub fn jsonStringify(self: @This(), jw: anytype) !void {
        try jw.beginObject();
        if (self.data) |value| {
            try jw.objectField("data");
            try jw.write(value);
        }
        if (self.start_time_ms) |value| {
            try jw.objectField("start_time_ms");
            try jw.write(value);
        }
        if (self.end_time_ms) |value| {
            try jw.objectField("end_time_ms");
            try jw.write(value);
        }
        if (self.frame_index) |value| {
            try jw.objectField("frame_index");
            try jw.write(value);
        }
        if (self.frame_delay_ms) |value| {
            try jw.objectField("frame_delay_ms");
            try jw.write(value);
        }
        try jw.endObject();
    }
};

/// A chunk of content. Text chunks have mime_type text/plain.
pub const Chunk = struct {
    /// The chunk text content
    text: ?[]const u8 = null,
    /// Character position in original text where chunk starts
    start_char: ?i64 = null,
    /// Character position in original text where chunk ends (exclusive)
    end_char: ?i64 = null,
    /// Base64-encoded binary data (valid WAV, PNG, etc.)
    data: ?[]const u8 = null,
    /// Audio: window start time in milliseconds
    start_time_ms: ?f32 = null,
    /// Audio: window end time in milliseconds
    end_time_ms: ?f32 = null,
    /// Animation: frame number
    frame_index: ?i64 = null,
    /// Animation: display delay in milliseconds
    frame_delay_ms: ?i64 = null,
    /// Sequence number of the chunk (0, 1, 2, ...)
    id: i64,
    /// MIME type: text/plain, audio/wav, image/png, etc.
    mime_type: []const u8,

    pub fn jsonParse(allocator: std.mem.Allocator, source: anytype, options: std.json.ParseOptions) !@This() {
        return try openApiParseObject(@This(), &.{
            .{ .json_name = "text", .zig_name = "text", .rejects_null = true },
            .{ .json_name = "start_char", .zig_name = "start_char", .rejects_null = true },
            .{ .json_name = "end_char", .zig_name = "end_char", .rejects_null = true },
            .{ .json_name = "data", .zig_name = "data", .rejects_null = true },
            .{ .json_name = "start_time_ms", .zig_name = "start_time_ms", .rejects_null = true },
            .{ .json_name = "end_time_ms", .zig_name = "end_time_ms", .rejects_null = true },
            .{ .json_name = "frame_index", .zig_name = "frame_index", .rejects_null = true },
            .{ .json_name = "frame_delay_ms", .zig_name = "frame_delay_ms", .rejects_null = true },
            .{ .json_name = "id", .zig_name = "id", .rejects_null = false },
            .{ .json_name = "mime_type", .zig_name = "mime_type", .rejects_null = false },
        }, allocator, source, options);
    }

    pub fn jsonParseFromValue(allocator: std.mem.Allocator, source: std.json.Value, options: std.json.ParseOptions) !@This() {
        return try openApiParseObjectFromValue(@This(), &.{
            .{ .json_name = "text", .zig_name = "text", .rejects_null = true },
            .{ .json_name = "start_char", .zig_name = "start_char", .rejects_null = true },
            .{ .json_name = "end_char", .zig_name = "end_char", .rejects_null = true },
            .{ .json_name = "data", .zig_name = "data", .rejects_null = true },
            .{ .json_name = "start_time_ms", .zig_name = "start_time_ms", .rejects_null = true },
            .{ .json_name = "end_time_ms", .zig_name = "end_time_ms", .rejects_null = true },
            .{ .json_name = "frame_index", .zig_name = "frame_index", .rejects_null = true },
            .{ .json_name = "frame_delay_ms", .zig_name = "frame_delay_ms", .rejects_null = true },
            .{ .json_name = "id", .zig_name = "id", .rejects_null = false },
            .{ .json_name = "mime_type", .zig_name = "mime_type", .rejects_null = false },
        }, allocator, source, options);
    }

    pub fn jsonStringify(self: @This(), jw: anytype) !void {
        try jw.beginObject();
        if (self.text) |value| {
            try jw.objectField("text");
            try jw.write(value);
        }
        if (self.start_char) |value| {
            try jw.objectField("start_char");
            try jw.write(value);
        }
        if (self.end_char) |value| {
            try jw.objectField("end_char");
            try jw.write(value);
        }
        if (self.data) |value| {
            try jw.objectField("data");
            try jw.write(value);
        }
        if (self.start_time_ms) |value| {
            try jw.objectField("start_time_ms");
            try jw.write(value);
        }
        if (self.end_time_ms) |value| {
            try jw.objectField("end_time_ms");
            try jw.write(value);
        }
        if (self.frame_index) |value| {
            try jw.objectField("frame_index");
            try jw.write(value);
        }
        if (self.frame_delay_ms) |value| {
            try jw.objectField("frame_delay_ms");
            try jw.write(value);
        }
        try jw.objectField("id");
        try jw.write(self.id);
        try jw.objectField("mime_type");
        try jw.write(self.mime_type);
        try jw.endObject();
    }
};

/// Per-request configuration for chunking. All fields are optional - zero/omitted values use chunker defaults.
pub const ChunkOptions = struct {
    /// Maximum number of chunks to generate per document.
    max_chunks: ?i64 = null,
    /// Confidence threshold for model-based chunking (0.0-1.0).
    threshold: ?f32 = null,
    text: ?TextChunkOptions = null,
    audio: ?AudioChunkOptions = null,

    pub fn jsonParse(allocator: std.mem.Allocator, source: anytype, options: std.json.ParseOptions) !@This() {
        return try openApiParseObject(@This(), &.{
            .{ .json_name = "max_chunks", .zig_name = "max_chunks", .rejects_null = true },
            .{ .json_name = "threshold", .zig_name = "threshold", .rejects_null = true },
            .{ .json_name = "text", .zig_name = "text", .rejects_null = true },
            .{ .json_name = "audio", .zig_name = "audio", .rejects_null = true },
        }, allocator, source, options);
    }

    pub fn jsonParseFromValue(allocator: std.mem.Allocator, source: std.json.Value, options: std.json.ParseOptions) !@This() {
        return try openApiParseObjectFromValue(@This(), &.{
            .{ .json_name = "max_chunks", .zig_name = "max_chunks", .rejects_null = true },
            .{ .json_name = "threshold", .zig_name = "threshold", .rejects_null = true },
            .{ .json_name = "text", .zig_name = "text", .rejects_null = true },
            .{ .json_name = "audio", .zig_name = "audio", .rejects_null = true },
        }, allocator, source, options);
    }

    pub fn jsonStringify(self: @This(), jw: anytype) !void {
        try jw.beginObject();
        if (self.max_chunks) |value| {
            try jw.objectField("max_chunks");
            try jw.write(value);
        }
        if (self.threshold) |value| {
            try jw.objectField("threshold");
            try jw.write(value);
        }
        if (self.text) |value| {
            try jw.objectField("text");
            try jw.write(value);
        }
        if (self.audio) |value| {
            try jw.objectField("audio");
            try jw.write(value);
        }
        try jw.endObject();
    }
};

/// Audio chunking configuration for inference, including VAD options.
pub const InferenceAudioChunkConfig = struct {
    /// Window duration in milliseconds for fixed-window audio chunking (default: 30000).
    window_duration_ms: ?i64 = null,
    /// Overlap duration in milliseconds between audio chunks (default: 0).
    overlap_duration_ms: ?i64 = null,
    vad: ?VADOptions = null,

    pub fn jsonParse(allocator: std.mem.Allocator, source: anytype, options: std.json.ParseOptions) !@This() {
        return try openApiParseObject(@This(), &.{
            .{ .json_name = "window_duration_ms", .zig_name = "window_duration_ms", .rejects_null = true },
            .{ .json_name = "overlap_duration_ms", .zig_name = "overlap_duration_ms", .rejects_null = true },
            .{ .json_name = "vad", .zig_name = "vad", .rejects_null = true },
        }, allocator, source, options);
    }

    pub fn jsonParseFromValue(allocator: std.mem.Allocator, source: std.json.Value, options: std.json.ParseOptions) !@This() {
        return try openApiParseObjectFromValue(@This(), &.{
            .{ .json_name = "window_duration_ms", .zig_name = "window_duration_ms", .rejects_null = true },
            .{ .json_name = "overlap_duration_ms", .zig_name = "overlap_duration_ms", .rejects_null = true },
            .{ .json_name = "vad", .zig_name = "vad", .rejects_null = true },
        }, allocator, source, options);
    }

    pub fn jsonStringify(self: @This(), jw: anytype) !void {
        try jw.beginObject();
        if (self.window_duration_ms) |value| {
            try jw.objectField("window_duration_ms");
            try jw.write(value);
        }
        if (self.overlap_duration_ms) |value| {
            try jw.objectField("overlap_duration_ms");
            try jw.write(value);
        }
        if (self.vad) |value| {
            try jw.objectField("vad");
            try jw.write(value);
        }
        try jw.endObject();
    }
};

/// Configuration for chunking requests to Antfly inference. Combines shared text options with inference-specific audio/VAD options.
pub const InferenceChunkConfig = struct {
    /// The chunking model to use. Either 'fixed' for simple token-based chunking, or a model name from models/chunkers/{name}/.
    model: ?[]const u8 = null,
    /// Maximum number of chunks to generate per document.
    max_chunks: ?i64 = null,
    /// Confidence threshold for model-based chunking (0.0-1.0). Used by ONNX text models and VAD audio models.
    threshold: ?f32 = null,
    text: ?TextChunkOptions = null,
    audio: ?InferenceAudioChunkConfig = null,

    pub fn jsonParse(allocator: std.mem.Allocator, source: anytype, options: std.json.ParseOptions) !@This() {
        return try openApiParseObject(@This(), &.{
            .{ .json_name = "model", .zig_name = "model", .rejects_null = true },
            .{ .json_name = "max_chunks", .zig_name = "max_chunks", .rejects_null = true },
            .{ .json_name = "threshold", .zig_name = "threshold", .rejects_null = true },
            .{ .json_name = "text", .zig_name = "text", .rejects_null = true },
            .{ .json_name = "audio", .zig_name = "audio", .rejects_null = true },
        }, allocator, source, options);
    }

    pub fn jsonParseFromValue(allocator: std.mem.Allocator, source: std.json.Value, options: std.json.ParseOptions) !@This() {
        return try openApiParseObjectFromValue(@This(), &.{
            .{ .json_name = "model", .zig_name = "model", .rejects_null = true },
            .{ .json_name = "max_chunks", .zig_name = "max_chunks", .rejects_null = true },
            .{ .json_name = "threshold", .zig_name = "threshold", .rejects_null = true },
            .{ .json_name = "text", .zig_name = "text", .rejects_null = true },
            .{ .json_name = "audio", .zig_name = "audio", .rejects_null = true },
        }, allocator, source, options);
    }

    pub fn jsonStringify(self: @This(), jw: anytype) !void {
        try jw.beginObject();
        if (self.model) |value| {
            try jw.objectField("model");
            try jw.write(value);
        }
        if (self.max_chunks) |value| {
            try jw.objectField("max_chunks");
            try jw.write(value);
        }
        if (self.threshold) |value| {
            try jw.objectField("threshold");
            try jw.write(value);
        }
        if (self.text) |value| {
            try jw.objectField("text");
            try jw.write(value);
        }
        if (self.audio) |value| {
            try jw.objectField("audio");
            try jw.write(value);
        }
        try jw.endObject();
    }
};

/// Content part supported by chunking requests.
pub const InferenceChunkContentPart = std.json.Value;

pub const InferenceChunkRequest = struct {
    /// Input content to chunk. Supports two formats: - Text string: `"This is a long document..."` - ContentPart: `{"type": "media", "data": "<base64>", "mime_type": "audio/wav"}` - ContentPart: `{"type": "text", "text": "..."}`
    input: std.json.Value,
    config: ?InferenceChunkConfig = null,

    pub fn jsonParse(allocator: std.mem.Allocator, source: anytype, options: std.json.ParseOptions) !@This() {
        return try openApiParseObject(@This(), &.{
            .{ .json_name = "input", .zig_name = "input", .rejects_null = false },
            .{ .json_name = "config", .zig_name = "config", .rejects_null = true },
        }, allocator, source, options);
    }

    pub fn jsonParseFromValue(allocator: std.mem.Allocator, source: std.json.Value, options: std.json.ParseOptions) !@This() {
        return try openApiParseObjectFromValue(@This(), &.{
            .{ .json_name = "input", .zig_name = "input", .rejects_null = false },
            .{ .json_name = "config", .zig_name = "config", .rejects_null = true },
        }, allocator, source, options);
    }

    pub fn jsonStringify(self: @This(), jw: anytype) !void {
        try jw.beginObject();
        try jw.objectField("input");
        try jw.write(self.input);
        if (self.config) |value| {
            try jw.objectField("config");
            try jw.write(value);
        }
        try jw.endObject();
    }
};

/// Options specific to text chunking.
pub const TextChunkOptions = struct {
    /// Target number of tokens per chunk.
    target_tokens: ?i64 = null,
    /// Number of tokens to overlap between consecutive chunks. Helps maintain context across chunk boundaries. Only used by fixed-size chunkers.
    overlap_tokens: ?i64 = null,
    /// Separator string for splitting (e.g., '\n\n' for paragraphs). Only used by fixed-size chunkers.
    separator: ?[]const u8 = null,

    pub fn jsonParse(allocator: std.mem.Allocator, source: anytype, options: std.json.ParseOptions) !@This() {
        return try openApiParseObject(@This(), &.{
            .{ .json_name = "target_tokens", .zig_name = "target_tokens", .rejects_null = true },
            .{ .json_name = "overlap_tokens", .zig_name = "overlap_tokens", .rejects_null = true },
            .{ .json_name = "separator", .zig_name = "separator", .rejects_null = true },
        }, allocator, source, options);
    }

    pub fn jsonParseFromValue(allocator: std.mem.Allocator, source: std.json.Value, options: std.json.ParseOptions) !@This() {
        return try openApiParseObjectFromValue(@This(), &.{
            .{ .json_name = "target_tokens", .zig_name = "target_tokens", .rejects_null = true },
            .{ .json_name = "overlap_tokens", .zig_name = "overlap_tokens", .rejects_null = true },
            .{ .json_name = "separator", .zig_name = "separator", .rejects_null = true },
        }, allocator, source, options);
    }

    pub fn jsonStringify(self: @This(), jw: anytype) !void {
        try jw.beginObject();
        if (self.target_tokens) |value| {
            try jw.objectField("target_tokens");
            try jw.write(value);
        }
        if (self.overlap_tokens) |value| {
            try jw.objectField("overlap_tokens");
            try jw.write(value);
        }
        if (self.separator) |value| {
            try jw.objectField("separator");
            try jw.write(value);
        }
        try jw.endObject();
    }
};

/// Text content with character offsets.
pub const TextContent = struct {
    /// The chunk text content
    text: []const u8,
    /// Character position in original text where chunk starts
    start_char: i64,
    /// Character position in original text where chunk ends (exclusive)
    end_char: i64,
};

/// Options for Voice Activity Detection (VAD) based audio segmentation.
pub const VADOptions = struct {
    /// Minimum silence duration (ms) to split speech segments. Gaps shorter than this are merged. Higher values produce longer, fewer segments. Default: 300.
    min_silence_duration_ms: ?i64 = null,
    /// Minimum speech duration (ms) for a segment to be kept. Shorter segments are discarded. Default: 250.
    min_speech_duration_ms: ?i64 = null,
    /// Padding (ms) added before and after detected speech. Default: 30.
    speech_pad_ms: ?i64 = null,
    /// Maximum segment duration (ms). Segments longer than this are split. Useful for Whisper-compatible chunking. Default: 30000.
    max_segment_duration_ms: ?i64 = null,

    pub fn jsonParse(allocator: std.mem.Allocator, source: anytype, options: std.json.ParseOptions) !@This() {
        return try openApiParseObject(@This(), &.{
            .{ .json_name = "min_silence_duration_ms", .zig_name = "min_silence_duration_ms", .rejects_null = true },
            .{ .json_name = "min_speech_duration_ms", .zig_name = "min_speech_duration_ms", .rejects_null = true },
            .{ .json_name = "speech_pad_ms", .zig_name = "speech_pad_ms", .rejects_null = true },
            .{ .json_name = "max_segment_duration_ms", .zig_name = "max_segment_duration_ms", .rejects_null = true },
        }, allocator, source, options);
    }

    pub fn jsonParseFromValue(allocator: std.mem.Allocator, source: std.json.Value, options: std.json.ParseOptions) !@This() {
        return try openApiParseObjectFromValue(@This(), &.{
            .{ .json_name = "min_silence_duration_ms", .zig_name = "min_silence_duration_ms", .rejects_null = true },
            .{ .json_name = "min_speech_duration_ms", .zig_name = "min_speech_duration_ms", .rejects_null = true },
            .{ .json_name = "speech_pad_ms", .zig_name = "speech_pad_ms", .rejects_null = true },
            .{ .json_name = "max_segment_duration_ms", .zig_name = "max_segment_duration_ms", .rejects_null = true },
        }, allocator, source, options);
    }

    pub fn jsonStringify(self: @This(), jw: anytype) !void {
        try jw.beginObject();
        if (self.min_silence_duration_ms) |value| {
            try jw.objectField("min_silence_duration_ms");
            try jw.write(value);
        }
        if (self.min_speech_duration_ms) |value| {
            try jw.objectField("min_speech_duration_ms");
            try jw.write(value);
        }
        if (self.speech_pad_ms) |value| {
            try jw.objectField("speech_pad_ms");
            try jw.write(value);
        }
        if (self.max_segment_duration_ms) |value| {
            try jw.objectField("max_segment_duration_ms");
            try jw.write(value);
        }
        try jw.endObject();
    }
};

const OpenApiObjectField = struct {
    json_name: []const u8,
    zig_name: []const u8,
    rejects_null: bool = false,
};

/// Parse an OpenAPI object without materializing a second JSON tree while
/// rejecting explicit null for optional properties whose schemas are non-nullable.
fn openApiParseObject(
    comptime T: type,
    comptime openapi_fields: []const OpenApiObjectField,
    allocator: std.mem.Allocator,
    source: anytype,
    options: std.json.ParseOptions,
) !T {
    @setEvalBranchQuota(100_000);
    const struct_info = @typeInfo(T).@"struct";
    if (struct_info.is_tuple) @compileError("OpenAPI object parser does not accept tuples");
    if (openapi_fields.len != struct_info.fields.len) @compileError("OpenAPI object field descriptors must match the generated struct");
    if (.object_begin != try source.next()) return error.UnexpectedToken;

    var result: T = undefined;
    var fields_seen = [_]bool{false} ** struct_info.fields.len;
    while (true) {
        var name_token: ?std.json.Token = try source.nextAllocMax(allocator, .alloc_if_needed, options.max_value_len.?);
        const field_name = switch (name_token.?) {
            inline .string, .allocated_string => |slice| slice,
            .object_end => break,
            else => return error.UnexpectedToken,
        };

        inline for (struct_info.fields, openapi_fields, 0..) |field, openapi_field, i| {
            if (field.is_comptime) @compileError("comptime fields are not supported: " ++ @typeName(T) ++ "." ++ field.name);
            if (comptime !std.mem.eql(u8, field.name, openapi_field.zig_name)) @compileError("OpenAPI object field descriptor order does not match the generated struct");
            if (std.mem.eql(u8, openapi_field.json_name, field_name)) {
                openApiFreeAllocatedToken(allocator, name_token.?);
                name_token = null;
                if (openapi_field.rejects_null and try source.peekNextTokenType() == .null) return error.UnexpectedToken;
                if (fields_seen[i]) {
                    switch (options.duplicate_field_behavior) {
                        .use_first => {
                            _ = try std.json.innerParse(field.type, allocator, source, options);
                            break;
                        },
                        .@"error" => return error.DuplicateField,
                        .use_last => {},
                    }
                }
                @field(result, field.name) = try std.json.innerParse(field.type, allocator, source, options);
                fields_seen[i] = true;
                break;
            }
        } else {
            openApiFreeAllocatedToken(allocator, name_token.?);
            if (options.ignore_unknown_fields) try source.skipValue() else return error.UnknownField;
        }
    }
    try openApiFillDefaultStructValues(T, openapi_fields, &result, &fields_seen);
    return result;
}

fn openApiParseObjectFromValue(
    comptime T: type,
    comptime openapi_fields: []const OpenApiObjectField,
    allocator: std.mem.Allocator,
    source: std.json.Value,
    options: std.json.ParseOptions,
) !T {
    @setEvalBranchQuota(100_000);
    const struct_info = @typeInfo(T).@"struct";
    if (struct_info.is_tuple) @compileError("OpenAPI object parser does not accept tuples");
    if (openapi_fields.len != struct_info.fields.len) @compileError("OpenAPI object field descriptors must match the generated struct");
    if (source != .object) return error.UnexpectedToken;
    var result: T = undefined;
    var fields_seen = [_]bool{false} ** struct_info.fields.len;
    var it = source.object.iterator();
    while (it.next()) |entry| {
        const field_name = entry.key_ptr.*;
        inline for (struct_info.fields, openapi_fields, 0..) |field, openapi_field, i| {
            if (field.is_comptime) @compileError("comptime fields are not supported: " ++ @typeName(T) ++ "." ++ field.name);
            if (comptime !std.mem.eql(u8, field.name, openapi_field.zig_name)) @compileError("OpenAPI object field descriptor order does not match the generated struct");
            if (std.mem.eql(u8, openapi_field.json_name, field_name)) {
                if (openapi_field.rejects_null and entry.value_ptr.* == .null) return error.UnexpectedToken;
                @field(result, field.name) = try std.json.innerParseFromValue(field.type, allocator, entry.value_ptr.*, options);
                fields_seen[i] = true;
                break;
            }
        } else if (!options.ignore_unknown_fields) return error.UnknownField;
    }
    try openApiFillDefaultStructValues(T, openapi_fields, &result, &fields_seen);
    return result;
}

fn openApiFillDefaultStructValues(comptime T: type, comptime openapi_fields: []const OpenApiObjectField, result: *T, fields_seen: *[@typeInfo(T).@"struct".fields.len]bool) !void {
    @setEvalBranchQuota(100_000);
    inline for (@typeInfo(T).@"struct".fields, openapi_fields, 0..) |field, openapi_field, i| {
        if (comptime !std.mem.eql(u8, field.name, openapi_field.zig_name)) @compileError("OpenAPI object field descriptor order does not match the generated struct");
        if (!fields_seen[i]) {
            if (field.defaultValue()) |default| @field(result, field.name) = default else return error.MissingField;
        }
    }
}

fn openApiFreeAllocatedToken(allocator: std.mem.Allocator, token: std.json.Token) void {
    switch (token) {
        .allocated_number, .allocated_string => |slice| allocator.free(slice),
        else => {},
    }
}
