// Code generated by openapi-zig. DO NOT EDIT.
// Package: tavily_api

const std = @import("std");

pub const ApiError = struct {
    detail: ?std.json.Value = null,

    /// OpenAPI wire names and nullability consumed by compatible typed JSON parsers.
    pub const openApiFieldMetadata = .{
        .{ "detail", "detail", true },
    };

    pub fn jsonParse(allocator: std.mem.Allocator, source: anytype, options: std.json.ParseOptions) !@This() {
        return try openApiParseObject(@This(), openApiFieldMetadata, allocator, source, options);
    }

    pub fn jsonParseFromValue(allocator: std.mem.Allocator, source: std.json.Value, options: std.json.ParseOptions) !@This() {
        return try openApiParseObjectFromValue(@This(), openApiFieldMetadata, allocator, source, options);
    }

    pub fn jsonStringify(self: @This(), jw: anytype) !void {
        try jw.beginObject();
        if (self.detail) |value| {
            try jw.objectField("detail");
            try jw.write(value);
        }
        try jw.endObject();
    }
};

pub const ResearchTaskCompleted = struct {
    /// The unique identifier of the research task.
    request_id: []const u8,
    /// Timestamp when the research task was created.
    created_at: []const u8,
    /// The current status of the research task.
    status: []const u8,
    /// The research report content. Can be a string or a structured object if output_schema was provided.
    content: std.json.Value,
    /// List of sources used in the research.
    sources: []const std.json.Value,
    /// Time in seconds it took to complete the request.
    response_time: i64,
    /// Credit usage details for the request.
    usage: ?std.json.ArrayHashMap(std.json.Value) = null,

    /// OpenAPI wire names and nullability consumed by compatible typed JSON parsers.
    pub const openApiFieldMetadata = .{
        .{ "request_id", "request_id", false },
        .{ "created_at", "created_at", false },
        .{ "status", "status", false },
        .{ "content", "content", false },
        .{ "sources", "sources", false },
        .{ "response_time", "response_time", false },
        .{ "usage", "usage", true },
    };

    pub fn jsonParse(allocator: std.mem.Allocator, source: anytype, options: std.json.ParseOptions) !@This() {
        return try openApiParseObject(@This(), openApiFieldMetadata, allocator, source, options);
    }

    pub fn jsonParseFromValue(allocator: std.mem.Allocator, source: std.json.Value, options: std.json.ParseOptions) !@This() {
        return try openApiParseObjectFromValue(@This(), openApiFieldMetadata, allocator, source, options);
    }

    pub fn jsonStringify(self: @This(), jw: anytype) !void {
        try jw.beginObject();
        try jw.objectField("request_id");
        try jw.write(self.request_id);
        try jw.objectField("created_at");
        try jw.write(self.created_at);
        try jw.objectField("status");
        try jw.write(self.status);
        try jw.objectField("content");
        try jw.write(self.content);
        try jw.objectField("sources");
        try jw.write(self.sources);
        try jw.objectField("response_time");
        try jw.write(self.response_time);
        if (self.usage) |value| {
            try jw.objectField("usage");
            try jw.write(value);
        }
        try jw.endObject();
    }
};

pub const ResearchTaskFailed = struct {
    /// The unique identifier of the research task.
    request_id: []const u8,
    /// The current status of the research task.
    status: []const u8,
    /// Time in seconds it took to complete the request.
    response_time: i64,
    /// Credit usage details for the request.
    usage: ?std.json.ArrayHashMap(std.json.Value) = null,

    /// OpenAPI wire names and nullability consumed by compatible typed JSON parsers.
    pub const openApiFieldMetadata = .{
        .{ "request_id", "request_id", false },
        .{ "status", "status", false },
        .{ "response_time", "response_time", false },
        .{ "usage", "usage", true },
    };

    pub fn jsonParse(allocator: std.mem.Allocator, source: anytype, options: std.json.ParseOptions) !@This() {
        return try openApiParseObject(@This(), openApiFieldMetadata, allocator, source, options);
    }

    pub fn jsonParseFromValue(allocator: std.mem.Allocator, source: std.json.Value, options: std.json.ParseOptions) !@This() {
        return try openApiParseObjectFromValue(@This(), openApiFieldMetadata, allocator, source, options);
    }

    pub fn jsonStringify(self: @This(), jw: anytype) !void {
        try jw.beginObject();
        try jw.objectField("request_id");
        try jw.write(self.request_id);
        try jw.objectField("status");
        try jw.write(self.status);
        try jw.objectField("response_time");
        try jw.write(self.response_time);
        if (self.usage) |value| {
            try jw.objectField("usage");
            try jw.write(value);
        }
        try jw.endObject();
    }
};

pub const SearchRequest = struct {
    /// The search query to execute with Tavily.
    query: []const u8,
    /// Controls the latency vs. relevance tradeoff and how `results[].content` is generated: - `advanced`: Highest relevance with increased latency. Best for detailed, high-precision queries. Returns multiple semantically relevant snippets per URL (configurable via `chunks_per_source`). - `basic`: A balanced option for relevance and latency. Ideal for general-purpose searches. Returns multiple semantically relevant snippets per URL (configurable via `chunks_per_source`). - `fast`: Prioritizes lower latency while maintaining good relevance. Returns multiple semantically relevant snippets per URL (configurable via `chunks_per_source`). - `ultra-fast`: Minimizes latency above all else. Best for time-critical use cases. Returns one NLP summary per URL. **Cost**: - `basic`, `fast`, `ultra-fast`: 1 API Credit - `advanced`: 2 API Credits See [Search Best Practices](/documentation/best-practices/best-practices-search#search-depth) for guidance on choosing the right search depth.
    search_depth: ?[]const u8 = null,
    /// Chunks are short content snippets (maximum 500 characters each) pulled directly from the source. Use `chunks_per_source` to define the maximum number of relevant chunks returned per source and to control the `content` length. Chunks will appear in the `content` field as: `<chunk 1> [...] <chunk 2> [...] <chunk 3>`. Available when `search_depth` is `advanced`, `basic` or `fast`.
    chunks_per_source: ?i64 = null,
    /// The maximum number of search results to return.
    max_results: ?i64 = null,
    /// The category of the search.`news` is useful for retrieving real-time updates, particularly about politics, sports, and major current events covered by mainstream media sources. `general` is for broader, more general-purpose searches that may include a wide range of sources.
    topic: ?[]const u8 = null,
    /// The time range back from the current date to filter results based on publish date or last updated date. Useful when looking for sources that have published or updated data. By default, results with no detectable published date are not removed; set `filter_by_published_date` to `true` to remove them.
    time_range: ?[]const u8 = null,
    /// Will return all results after the specified start date based on publish date or last updated date. Required to be written in the format YYYY-MM-DD. By default, results with no detectable published date are not removed; set `filter_by_published_date` to `true` to remove them.
    start_date: ?[]const u8 = null,
    /// Will return all results before the specified end date based on publish date or last updated date. Required to be written in the format YYYY-MM-DD. By default, results with no detectable published date are not removed; set `filter_by_published_date` to `true` to remove them.
    end_date: ?[]const u8 = null,
    /// Include a `published_date` field in each result. The date is Tavily's best estimate of when the source was published or last updated, so it can be later than the original publish date. Results with no detectable date return `null`. Automatically enabled when `topic` is `news`. **Note**: this feature is currently in beta.
    include_published_date: ?bool = null,
    /// Remove results whose published date falls outside the `time_range`, `start_date`, or `end_date` window. Results with no detectable published date are also removed. Setting this to `true` also enables `include_published_date`. If you want date-window filtering but don't want to lose sources without a detectable date, leave this `false` and set `include_published_date: true` together with a date range instead.
    filter_by_published_date: ?bool = null,
    /// Include an LLM-generated answer to the provided query. `basic` or `true` returns a quick answer. `advanced` returns a more detailed answer.
    include_answer: ?std.json.Value = null,
    /// Include the cleaned and parsed HTML content of each search result. `markdown` or `true` returns search result content in markdown format. `text` returns the plain text from the results and may increase latency.
    include_raw_content: ?std.json.Value = null,
    /// Include images in the response. Returns both a top-level `images` list of query-related images and an `images` array inside each result object with images extracted from that specific source.
    include_images: ?bool = null,
    /// When `include_images` is `true`, also add a descriptive text for each image.
    include_image_descriptions: ?bool = null,
    /// Whether to include the favicon URL for each result.
    include_favicon: ?bool = null,
    /// A list of domains to specifically include in the search results. Maximum 300 domains.
    include_domains: ?[]const []const u8 = null,
    /// A list of domains to specifically exclude from the search results. Maximum 150 domains.
    exclude_domains: ?[]const []const u8 = null,
    /// Controls how `include_domains` is applied. `restrict` limits results to only the listed domains. `prefer` also searches the rest of the web, so results outside `include_domains` can still surface, rather than excluding them. Requires `include_domains` to be set; returns a 400 error otherwise.
    include_domains_mode: ?[]const u8 = null,
    /// Boost search results from a specific country. This will prioritize content from the selected country in the search results. Available only if topic is `general`.
    country: ?[]const u8 = null,
    /// Boost search results in a specific language. Accepts an ISO 639-1 code (e.g. `en`, `fr`, `zh-cn`) or an English language name (e.g. `english`, `french`). By default this only boosts matching-language results in ranking; pass `filter_by_language: true` to strictly filter out non-matching results instead. For best results, write your `query` in the same language you set here.
    language: ?[]const u8 = null,
    /// Strictly filter out search results that don't match the `language` parameter, instead of only boosting them in ranking. Requires `language` to be set; returns a 400 error otherwise.
    filter_by_language: ?bool = null,
    /// When `auto_parameters` is enabled, Tavily automatically configures search parameters based on your query's content and intent. You can still set other parameters manually, and your explicit values will override the automatic ones. The parameters `include_answer`, `include_raw_content`, and `max_results` must always be set manually, as they directly affect response size. Note: `search_depth` may be automatically set to advanced when it's likely to improve results. This uses 2 API credits per request. To avoid the extra cost, you can explicitly set `search_depth` to `basic`.
    auto_parameters: ?bool = null,
    /// Ensure that only search results containing the exact quoted phrase(s) in the query are returned, bypassing synonyms or semantic variations. Wrap target phrases in quotes within your query (e.g. `"John Smith" CEO Acme Corp`). Punctuation is typically ignored inside quotes.
    exact_match: ?bool = null,
    /// Whether to include credit usage information in the response.
    include_usage: ?bool = null,
    /// Whether to filter out adult or unsafe content from results. Not supported for `fast` or `ultra-fast` search depths.
    safe_search: ?bool = null,

    /// OpenAPI wire names and nullability consumed by compatible typed JSON parsers.
    pub const openApiFieldMetadata = .{
        .{ "query", "query", false },
        .{ "search_depth", "search_depth", true },
        .{ "chunks_per_source", "chunks_per_source", true },
        .{ "max_results", "max_results", true },
        .{ "topic", "topic", true },
        .{ "time_range", "time_range", true },
        .{ "start_date", "start_date", true },
        .{ "end_date", "end_date", true },
        .{ "include_published_date", "include_published_date", true },
        .{ "filter_by_published_date", "filter_by_published_date", true },
        .{ "include_answer", "include_answer", true },
        .{ "include_raw_content", "include_raw_content", true },
        .{ "include_images", "include_images", true },
        .{ "include_image_descriptions", "include_image_descriptions", true },
        .{ "include_favicon", "include_favicon", true },
        .{ "include_domains", "include_domains", true },
        .{ "exclude_domains", "exclude_domains", true },
        .{ "include_domains_mode", "include_domains_mode", true },
        .{ "country", "country", true },
        .{ "language", "language", true },
        .{ "filter_by_language", "filter_by_language", true },
        .{ "auto_parameters", "auto_parameters", true },
        .{ "exact_match", "exact_match", true },
        .{ "include_usage", "include_usage", true },
        .{ "safe_search", "safe_search", true },
    };

    pub fn jsonParse(allocator: std.mem.Allocator, source: anytype, options: std.json.ParseOptions) !@This() {
        return try openApiParseObject(@This(), openApiFieldMetadata, allocator, source, options);
    }

    pub fn jsonParseFromValue(allocator: std.mem.Allocator, source: std.json.Value, options: std.json.ParseOptions) !@This() {
        return try openApiParseObjectFromValue(@This(), openApiFieldMetadata, allocator, source, options);
    }

    pub fn jsonStringify(self: @This(), jw: anytype) !void {
        try jw.beginObject();
        try jw.objectField("query");
        try jw.write(self.query);
        if (self.search_depth) |value| {
            try jw.objectField("search_depth");
            try jw.write(value);
        }
        if (self.chunks_per_source) |value| {
            try jw.objectField("chunks_per_source");
            try jw.write(value);
        }
        if (self.max_results) |value| {
            try jw.objectField("max_results");
            try jw.write(value);
        }
        if (self.topic) |value| {
            try jw.objectField("topic");
            try jw.write(value);
        }
        if (self.time_range) |value| {
            try jw.objectField("time_range");
            try jw.write(value);
        }
        if (self.start_date) |value| {
            try jw.objectField("start_date");
            try jw.write(value);
        }
        if (self.end_date) |value| {
            try jw.objectField("end_date");
            try jw.write(value);
        }
        if (self.include_published_date) |value| {
            try jw.objectField("include_published_date");
            try jw.write(value);
        }
        if (self.filter_by_published_date) |value| {
            try jw.objectField("filter_by_published_date");
            try jw.write(value);
        }
        if (self.include_answer) |value| {
            try jw.objectField("include_answer");
            try jw.write(value);
        }
        if (self.include_raw_content) |value| {
            try jw.objectField("include_raw_content");
            try jw.write(value);
        }
        if (self.include_images) |value| {
            try jw.objectField("include_images");
            try jw.write(value);
        }
        if (self.include_image_descriptions) |value| {
            try jw.objectField("include_image_descriptions");
            try jw.write(value);
        }
        if (self.include_favicon) |value| {
            try jw.objectField("include_favicon");
            try jw.write(value);
        }
        if (self.include_domains) |value| {
            try jw.objectField("include_domains");
            try jw.write(value);
        }
        if (self.exclude_domains) |value| {
            try jw.objectField("exclude_domains");
            try jw.write(value);
        }
        if (self.include_domains_mode) |value| {
            try jw.objectField("include_domains_mode");
            try jw.write(value);
        }
        if (self.country) |value| {
            try jw.objectField("country");
            try jw.write(value);
        }
        if (self.language) |value| {
            try jw.objectField("language");
            try jw.write(value);
        }
        if (self.filter_by_language) |value| {
            try jw.objectField("filter_by_language");
            try jw.write(value);
        }
        if (self.auto_parameters) |value| {
            try jw.objectField("auto_parameters");
            try jw.write(value);
        }
        if (self.exact_match) |value| {
            try jw.objectField("exact_match");
            try jw.write(value);
        }
        if (self.include_usage) |value| {
            try jw.objectField("include_usage");
            try jw.write(value);
        }
        if (self.safe_search) |value| {
            try jw.objectField("safe_search");
            try jw.write(value);
        }
        try jw.endObject();
    }
};

pub const SearchResult = struct {
    /// The title of the search result.
    title: ?[]const u8 = null,
    /// The URL of the search result.
    url: ?[]const u8 = null,
    /// A short description of the search result.
    content: ?[]const u8 = null,
    /// The relevance score of the search result.
    score: ?f32 = null,
    /// The cleaned and parsed HTML content of the search result. Only if `include_raw_content` is true.
    raw_content: ?[]const u8 = null,
    /// Tavily's best estimate of when the source was published or last updated. Can be later than the original publish date. `null` if no date could be detected. Only returned when `include_published_date` is `true` (automatically enabled when `topic` is `news`).
    published_date: ?[]const u8 = null,
    /// The favicon URL for the result.
    favicon: ?[]const u8 = null,
    /// A list of images extracted from this search result. Only included when `include_images` is `true`. If `include_image_descriptions` is `true`, each item will have `url` and `description`.
    images: ?[]const std.json.Value = null,
    /// Unique identifier for this result
    id: ?[]const u8 = null,

    /// OpenAPI wire names and nullability consumed by compatible typed JSON parsers.
    pub const openApiFieldMetadata = .{
        .{ "title", "title", true },
        .{ "url", "url", true },
        .{ "content", "content", true },
        .{ "score", "score", true },
        .{ "raw_content", "raw_content", true },
        .{ "published_date", "published_date", true },
        .{ "favicon", "favicon", true },
        .{ "images", "images", true },
        .{ "id", "id", true },
    };

    pub fn jsonParse(allocator: std.mem.Allocator, source: anytype, options: std.json.ParseOptions) !@This() {
        return try openApiParseObject(@This(), openApiFieldMetadata, allocator, source, options);
    }

    pub fn jsonParseFromValue(allocator: std.mem.Allocator, source: std.json.Value, options: std.json.ParseOptions) !@This() {
        return try openApiParseObjectFromValue(@This(), openApiFieldMetadata, allocator, source, options);
    }

    pub fn jsonStringify(self: @This(), jw: anytype) !void {
        try jw.beginObject();
        if (self.title) |value| {
            try jw.objectField("title");
            try jw.write(value);
        }
        if (self.url) |value| {
            try jw.objectField("url");
            try jw.write(value);
        }
        if (self.content) |value| {
            try jw.objectField("content");
            try jw.write(value);
        }
        if (self.score) |value| {
            try jw.objectField("score");
            try jw.write(value);
        }
        if (self.raw_content) |value| {
            try jw.objectField("raw_content");
            try jw.write(value);
        }
        if (self.published_date) |value| {
            try jw.objectField("published_date");
            try jw.write(value);
        }
        if (self.favicon) |value| {
            try jw.objectField("favicon");
            try jw.write(value);
        }
        if (self.images) |value| {
            try jw.objectField("images");
            try jw.write(value);
        }
        if (self.id) |value| {
            try jw.objectField("id");
            try jw.write(value);
        }
        try jw.endObject();
    }
};

/// Breakdown per request type
pub const UsageByType = struct {
    search: ?UsageMetrics = null,
    crawl: ?UsageMetrics = null,
    extract: ?UsageMetrics = null,
    map: ?UsageMetrics = null,
    research: ?UsageMetrics = null,

    /// OpenAPI wire names and nullability consumed by compatible typed JSON parsers.
    pub const openApiFieldMetadata = .{
        .{ "search", "search", true },
        .{ "crawl", "crawl", true },
        .{ "extract", "extract", true },
        .{ "map", "map", true },
        .{ "research", "research", true },
    };

    pub fn jsonParse(allocator: std.mem.Allocator, source: anytype, options: std.json.ParseOptions) !@This() {
        return try openApiParseObject(@This(), openApiFieldMetadata, allocator, source, options);
    }

    pub fn jsonParseFromValue(allocator: std.mem.Allocator, source: std.json.Value, options: std.json.ParseOptions) !@This() {
        return try openApiParseObjectFromValue(@This(), openApiFieldMetadata, allocator, source, options);
    }

    pub fn jsonStringify(self: @This(), jw: anytype) !void {
        try jw.beginObject();
        if (self.search) |value| {
            try jw.objectField("search");
            try jw.write(value);
        }
        if (self.crawl) |value| {
            try jw.objectField("crawl");
            try jw.write(value);
        }
        if (self.extract) |value| {
            try jw.objectField("extract");
            try jw.write(value);
        }
        if (self.map) |value| {
            try jw.objectField("map");
            try jw.write(value);
        }
        if (self.research) |value| {
            try jw.objectField("research");
            try jw.write(value);
        }
        try jw.endObject();
    }
};

pub const UsageMetrics = struct {
    /// Credits consumed
    usage: ?i64 = null,
    /// Pay-as-you-go cost in USD (overage credits billed at the account's per-credit rate)
    paygo_cost_usd: ?f64 = null,
    /// Number of requests
    request_count: ?i64 = null,

    /// OpenAPI wire names and nullability consumed by compatible typed JSON parsers.
    pub const openApiFieldMetadata = .{
        .{ "usage", "usage", true },
        .{ "paygo_cost_usd", "paygo_cost_usd", true },
        .{ "request_count", "request_count", true },
    };

    pub fn jsonParse(allocator: std.mem.Allocator, source: anytype, options: std.json.ParseOptions) !@This() {
        return try openApiParseObject(@This(), openApiFieldMetadata, allocator, source, options);
    }

    pub fn jsonParseFromValue(allocator: std.mem.Allocator, source: std.json.Value, options: std.json.ParseOptions) !@This() {
        return try openApiParseObjectFromValue(@This(), openApiFieldMetadata, allocator, source, options);
    }

    pub fn jsonStringify(self: @This(), jw: anytype) !void {
        try jw.beginObject();
        if (self.usage) |value| {
            try jw.objectField("usage");
            try jw.write(value);
        }
        if (self.paygo_cost_usd) |value| {
            try jw.objectField("paygo_cost_usd");
            try jw.write(value);
        }
        if (self.request_count) |value| {
            try jw.objectField("request_count");
            try jw.write(value);
        }
        try jw.endObject();
    }
};

pub const ValidationError = struct {
    detail: ?[]const std.json.Value = null,

    /// OpenAPI wire names and nullability consumed by compatible typed JSON parsers.
    pub const openApiFieldMetadata = .{
        .{ "detail", "detail", true },
    };

    pub fn jsonParse(allocator: std.mem.Allocator, source: anytype, options: std.json.ParseOptions) !@This() {
        return try openApiParseObject(@This(), openApiFieldMetadata, allocator, source, options);
    }

    pub fn jsonParseFromValue(allocator: std.mem.Allocator, source: std.json.Value, options: std.json.ParseOptions) !@This() {
        return try openApiParseObjectFromValue(@This(), openApiFieldMetadata, allocator, source, options);
    }

    pub fn jsonStringify(self: @This(), jw: anytype) !void {
        try jw.beginObject();
        if (self.detail) |value| {
            try jw.objectField("detail");
            try jw.write(value);
        }
        try jw.endObject();
    }
};

pub const OpenApiGetResearchResponse200 = union(enum) {
    research_task_completed: ResearchTaskCompleted,
    research_task_failed: ResearchTaskFailed,

    pub fn jsonParseFromSliceLeaky(allocator: std.mem.Allocator, input: []const u8, options: std.json.ParseOptions) !@This() {
        const DiscriminatorProbe = union(enum) {
            missing,
            value: []const u8,
            pub fn jsonParse(probe_allocator: std.mem.Allocator, probe_source: anytype, probe_options: std.json.ParseOptions) !@This() {
                return .{ .value = try std.json.innerParse([]const u8, probe_allocator, probe_source, probe_options) };
            }
        };
        const Probe = struct { status: DiscriminatorProbe = .missing };
        var probe_options = options;
        probe_options.ignore_unknown_fields = true;
        const probe = try std.json.parseFromSliceLeaky(Probe, allocator, input, probe_options);
        const disc_str = switch (probe.status) {
            .value => |value| value,
            .missing => {
                return error.MissingField;
            },
        };
        if (std.mem.eql(u8, disc_str, "completed")) {
            return .{ .research_task_completed = try std.json.parseFromSliceLeaky(ResearchTaskCompleted, allocator, input, options) };
        }
        if (std.mem.eql(u8, disc_str, "failed")) {
            return .{ .research_task_failed = try std.json.parseFromSliceLeaky(ResearchTaskFailed, allocator, input, options) };
        }
        return error.UnexpectedToken;
    }

    pub fn jsonParse(allocator: std.mem.Allocator, source: anytype, options: std.json.ParseOptions) !@This() {
        const value = try std.json.innerParse(std.json.Value, allocator, source, options);
        return try jsonParseFromValue(allocator, value, options);
    }

    pub fn jsonParseFromValue(allocator: std.mem.Allocator, source: std.json.Value, options: std.json.ParseOptions) !@This() {
        if (source != .object) return error.UnexpectedToken;
        const disc_val = source.object.get("status") orelse {
            return error.MissingField;
        };
        const disc_str = switch (disc_val) {
            .string => |s| s,
            else => return error.UnexpectedToken,
        };
        if (std.mem.eql(u8, disc_str, "completed")) {
            return .{ .research_task_completed = try std.json.parseFromValueLeaky(ResearchTaskCompleted, allocator, source, options) };
        }
        if (std.mem.eql(u8, disc_str, "failed")) {
            return .{ .research_task_failed = try std.json.parseFromValueLeaky(ResearchTaskFailed, allocator, source, options) };
        }
        return error.UnexpectedToken;
    }

    pub fn jsonStringify(self: @This(), jw: anytype) !void {
        switch (self) {
            .research_task_completed => |v| try jw.write(v),
            .research_task_failed => |v| try jw.write(v),
        }
    }
};

pub const OpenApiGetResearchResponse202 = struct {
    /// The unique identifier of the research task.
    request_id: []const u8,
    /// Current status of the research task.
    status: []const u8,
    /// Time in seconds it took to complete the request.
    response_time: i64,
};

/// Parse an OpenAPI object without materializing a second JSON tree while
/// rejecting explicit null for optional properties whose schemas are non-nullable.
fn openApiParseObject(
    comptime T: type,
    comptime openapi_fields: anytype,
    allocator: std.mem.Allocator,
    source: anytype,
    options: std.json.ParseOptions,
) !T {
    @setEvalBranchQuota(100_000);
    const struct_info = @typeInfo(T).@"struct";
    if (struct_info.is_tuple) @compileError("OpenAPI object parser does not accept tuples");
    if (openapi_fields.len != struct_info.fields.len) @compileError("OpenAPI object field descriptors must match the generated struct");
    if (.object_begin != try source.next()) return error.UnexpectedToken;

    var result: T = undefined;
    var fields_seen = [_]bool{false} ** struct_info.fields.len;
    while (true) {
        var name_token: ?std.json.Token = try source.nextAllocMax(allocator, .alloc_if_needed, options.max_value_len.?);
        const field_name = switch (name_token.?) {
            inline .string, .allocated_string => |slice| slice,
            .object_end => break,
            else => return error.UnexpectedToken,
        };

        inline for (struct_info.fields, openapi_fields, 0..) |field, openapi_field, i| {
            if (field.is_comptime) @compileError("comptime fields are not supported: " ++ @typeName(T) ++ "." ++ field.name);
            if (comptime !std.mem.eql(u8, field.name, openapi_field[1])) @compileError("OpenAPI object field descriptor order does not match the generated struct");
            if (std.mem.eql(u8, openapi_field[0], field_name)) {
                openApiFreeAllocatedToken(allocator, name_token.?);
                name_token = null;
                if (openapi_field[2] and try source.peekNextTokenType() == .null) return error.UnexpectedToken;
                if (fields_seen[i]) {
                    switch (options.duplicate_field_behavior) {
                        .use_first => {
                            _ = try std.json.innerParse(field.type, allocator, source, options);
                            break;
                        },
                        .@"error" => return error.DuplicateField,
                        .use_last => {},
                    }
                }
                @field(result, field.name) = try std.json.innerParse(field.type, allocator, source, options);
                fields_seen[i] = true;
                break;
            }
        } else {
            openApiFreeAllocatedToken(allocator, name_token.?);
            if (options.ignore_unknown_fields) try source.skipValue() else return error.UnknownField;
        }
    }
    try openApiFillDefaultStructValues(T, openapi_fields, &result, &fields_seen);
    return result;
}

fn openApiParseObjectFromValue(
    comptime T: type,
    comptime openapi_fields: anytype,
    allocator: std.mem.Allocator,
    source: std.json.Value,
    options: std.json.ParseOptions,
) !T {
    @setEvalBranchQuota(100_000);
    const struct_info = @typeInfo(T).@"struct";
    if (struct_info.is_tuple) @compileError("OpenAPI object parser does not accept tuples");
    if (openapi_fields.len != struct_info.fields.len) @compileError("OpenAPI object field descriptors must match the generated struct");
    if (source != .object) return error.UnexpectedToken;
    var result: T = undefined;
    var fields_seen = [_]bool{false} ** struct_info.fields.len;
    var it = source.object.iterator();
    while (it.next()) |entry| {
        const field_name = entry.key_ptr.*;
        inline for (struct_info.fields, openapi_fields, 0..) |field, openapi_field, i| {
            if (field.is_comptime) @compileError("comptime fields are not supported: " ++ @typeName(T) ++ "." ++ field.name);
            if (comptime !std.mem.eql(u8, field.name, openapi_field[1])) @compileError("OpenAPI object field descriptor order does not match the generated struct");
            if (std.mem.eql(u8, openapi_field[0], field_name)) {
                if (openapi_field[2] and entry.value_ptr.* == .null) return error.UnexpectedToken;
                @field(result, field.name) = try std.json.innerParseFromValue(field.type, allocator, entry.value_ptr.*, options);
                fields_seen[i] = true;
                break;
            }
        } else if (!options.ignore_unknown_fields) return error.UnknownField;
    }
    try openApiFillDefaultStructValues(T, openapi_fields, &result, &fields_seen);
    return result;
}

fn openApiFillDefaultStructValues(comptime T: type, comptime openapi_fields: anytype, result: *T, fields_seen: *[@typeInfo(T).@"struct".fields.len]bool) !void {
    @setEvalBranchQuota(100_000);
    inline for (@typeInfo(T).@"struct".fields, openapi_fields, 0..) |field, openapi_field, i| {
        if (comptime !std.mem.eql(u8, field.name, openapi_field[1])) @compileError("OpenAPI object field descriptor order does not match the generated struct");
        if (!fields_seen[i]) {
            if (field.defaultValue()) |default| @field(result, field.name) = default else return error.MissingField;
        }
    }
}

fn openApiFreeAllocatedToken(allocator: std.mem.Allocator, token: std.json.Token) void {
    switch (token) {
        .allocated_number, .allocated_string => |slice| allocator.free(slice),
        else => {},
    }
}
