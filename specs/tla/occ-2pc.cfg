SPECIFICATION Spec

\* Small model for tractable checking
CONSTANTS
    Txns = {t1, t2}
    Shards = {s1, s2}
    Keys = {k1}
    Pending = "pending"
    Committed = "committed"
    Aborted = "aborted"

INVARIANTS
    TypeInvariant
    NoOrphanedIntents
    NoOrphanedCommittedIntents
    SerializableReads
